# cairoon API Inventory

This file is the migration ledger for turning pycairo into a MoonBit Cairo
binding. Treat `cairo/__init__.pyi`, `docs/reference/*.rst`, and
`tests/test_*.py` in the parent pycairo repository as the source of truth.

## Legend

- Done: implemented, tested, documented, and included in `src/pkg.generated.mbti`.
- Partial: some API or behavior exists, but feature parity is not proven.
- Todo: no MoonBit binding yet.
- Decision: product scope or platform policy must be pinned before
  implementation.

## Recent Audit Deltas

- 2026-07-08: The external `src/tests/api/pycairo` package now carries the
  pycairo `test_api.py::test_surface_file_obj_error` fixture, proving failing
  PDF/PS stream writers map to checked `CairoIOError(WriteError, _)` through
  the published package path.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 61,
  a link-audit file/stream/direct-C oracle covering PDF metadata/custom
  metadata, page labels, nested outlines, URI links, named-destination links,
  list-style structure tags, PS DSC markers, SVG pixel units, and PS/SVG
  negative PDF tag-metadata checks.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 60,
  a cross-reference file/stream/direct-C oracle covering PDF metadata/custom
  metadata overwrite-removal, page labels, nested outlines, named-destination
  links, URI links, Index/Reference/Figure/Caption structure tags, and mixed
  `Surface::show_page`/`Surface::copy_page` transitions.
- 2026-07-08: The external `src/tests/backend/ps` package now carries a
  pycairo `test_surface.py::test_ps_surface_misc` DSC setup-order fixture,
  proving `ps_dsc_begin_page_setup` followed by `ps_dsc_begin_setup` through the
  published package path.
- 2026-07-08: The external `src/tests/api/pycairo` package now carries the
  pycairo `test_api.py::test_surface` constructor smoke fixture, proving image
  dimensions/format, PDF/PS/SVG stream constructors, and bounded/unbounded
  recording constructors through the published package path.
- 2026-07-08: The external `src/tests/api/pycairo` package now carries the
  pycairo `test_api.py::test_recording_surface_get_extents` fixture, proving
  unbounded, bounded, and post-finish recording extents through the published
  package path.
- 2026-07-08: pycairo `test_device.py::test_script_device_to_path` parity now
  verifies `Device::script(path)` writes script content to the requested
  pathname and cleans the temporary output around the fixture.
- 2026-07-08: The layout gate now enforces native-only target entries for every
  production `src/**/ffi*.mbt` file, including internal helper packages, so raw
  FFI declarations cannot silently become backend-generic during package
  extraction.
- 2026-07-08: PDF custom metadata parity now asserts that pycairo's reserved
  custom metadata names map to Cairo `InvalidString`, preserve
  `Status::message`, and use the checked `CairoInvalidArgument` payload.
- 2026-07-08: ARGB32 image rendering oracle coverage now includes a
  forty-fourth ordinary and buffer-backed direct-C scene in the
  pattern-operator oracle file, combining a transformed repeated surface
  pattern, a reflected transformed linear gradient through `OperatorHslHue`,
  group compositing, and a transformed radial mask.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 59,
  a research-note file/stream/direct-C oracle covering PDF metadata/custom
  metadata overwrite-removal, page labels, nested outlines, URI and
  named-destination links, table/list/note/reference/figure/caption structure
  tags, and mixed `Context::copy_page`/`Surface::copy_page`/show-page
  transitions.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 58,
  an appendix-rubric file/stream/direct-C oracle covering PDF metadata/custom
  metadata overwrite-removal, page labels, nested outlines, URI and
  named-destination links, Index/Code/Formula/Figure/Caption/BibEntry
  structure tags, and mixed `Context::copy_page`/`Surface::copy_page`/show-page
  transitions.
- 2026-07-08: Context pycairo parity now includes pixel-level coverage for
  `Context::set_source_surface` default and explicit offsets plus
  `Context::mask_surface` explicit offsets.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 57,
  a review-dossier file/stream/direct-C oracle covering PDF metadata/custom
  metadata overwrite-removal, page labels, nested outlines, URI and
  named-destination links, TOC/list/table/note structure tags, and
  copy/show-page transitions.
- 2026-07-08: PDF/PS/SVG backend differential coverage now includes scene 56,
  an article-thread file/stream/direct-C oracle covering PDF metadata/custom
  metadata overwrite-removal, page labels, nested outlines, URI and
  named-destination tags, article/block-quote/note/figure/caption/reference
  structure tags, and copy/show page transitions.
- 2026-07-08: pycairo `test_image_surface_release_on_finish` parity now
  releases the retained MoonBit buffer for `Surface::image_for_data` during
  `Surface::finish`; a black-box test probes the internal Cairo user-data slot,
  then checks `copy_data` still maps to `SurfaceFinished`.
- 2026-07-07: pycairo `test_api.py::test_version` parity now includes the
  `CAIRO_VERSION_STRING == major.minor.micro` component consistency check and
  the Cairo encoded integer formula for `CAIRO_VERSION`.
- 2026-07-07: ARGB32 image rendering oracle coverage now includes a
  forty-third ordinary and buffer-backed direct-C scene in a separate
  pattern-operator oracle file, combining a transformed reflected surface
  pattern with a transformed radial gradient through `OperatorColorDodge`.
- 2026-07-07: PDF/PS/SVG backend differential coverage now includes scene 54,
  a bookmark-lattice file/stream/direct-C oracle covering metadata/custom
  metadata overwrite-removal, page labels, outline parent-child/sibling
  structure, URI and named-destination tags, and copy/show page transitions.
- 2026-07-07: ARGB32 image rendering oracle coverage now includes a
  forty-second ordinary and buffer-backed direct-C scene combining a transformed
  repeated surface-pattern stroke, linear paint, radial mask, group
  compositing, and `OperatorDifference`.
- 2026-07-07: ARGB32 image rendering oracle coverage now includes a
  forty-first ordinary and buffer-backed direct-C scene combining a mesh
  pattern, transformed surface pattern, repeated linear gradient, radial mask,
  group compositing, and `OperatorSoftLight`.
- 2026-07-07: PDF stream tag/metadata reliability now includes a public
  black-box marker fixture covering PDF stream output with metadata, custom
  metadata, page labels, parent/child outlines, URI links, named destinations,
  and document-structure tags through the published `CAIMEOX/cairoon` package.
- 2026-07-07: pycairo `test_surface.py` parity now includes a compact
  `ImageSurface.create_for_data` array fixture: a prefilled ARGB32
  `FixedArray[Byte]` can be wrapped with the default stride, queried for
  width/height/stride, and copied back byte-for-byte from the surface.
- 2026-07-07: pycairo `test_surface.py` parity now includes
  `test_image_surface_get_data_empty`: an ordinary 0x0 image surface exposes
  an empty `ImageData` view and empty copied data.
- 2026-07-07: pycairo `test_surface.py` parity now includes a
  `Surface::script_for_target` target-paint fixture: drawing through a
  script surface writes script output and produces the same image bytes as
  drawing directly to the target image surface.
- 2026-07-07: pycairo `test_api.py` parity now includes the
  `test_recording_surface_get_extents` post-finish fixture. Recording-surface
  bounded extents stay readable after `Surface::finish`, while non-recording
  subtype checks still map to `SurfaceTypeMismatch`.
- 2026-07-07: pycairo `test_api.py` parity now includes the
  `test_image_surface_get_data` mutable data-view fixture: an `ImageData` view
  shares bytes with later `get_data()` calls and observes subsequent
  `Context::paint` plus `Surface::flush` updates.
- 2026-07-07: pycairo `test_api.py` parity now includes
  `Surface::has_show_text_glyphs` before and after `finish()` plus the
  `Context::paint`/`OperatorSource` smoke fixture, covering the finished-surface
  `SurfaceFinished` mapping and deterministic ARGB32 white output.
- 2026-07-07: pycairo `test_api.py` parity now includes the
  `surface_destroy_before_context` and
  `surface_destroy_before_surface_pattern` lifetime fixtures, covering PDF/PS
  stream targets retained by `Context` and PDF stream surfaces retained through
  `Pattern::for_surface`.
- 2026-07-07: pycairo `test_api.py` parity now includes a Unicode
  `Context::show_text` smoke fixture and a Unicode PNG filename round-trip
  fixture through `Surface::write_to_png` and `Surface::image_from_png`.
- 2026-07-07: Backend vector oracle coverage adds scene 53, a cross-backend
  semantic-index fixture with PDF metadata, custom metadata, page labels,
  nested outlines, destination and URI links, TOC/TOCI/Reference,
  Art/BlockQuote/NonStruct/Note, and Figure/Caption structure tags, retained
  `Surface::copy_page`, stream-to-direct-C oracle equality, stable marker
  checks, and PS/SVG negative PDF-metadata assertions.
- 2026-07-07: Backend vector oracle coverage adds scene 52, a cross-backend
  annotation/list/table sequence with PDF metadata, custom metadata, page
  labels, outlines, URI and destination links, list/table structure tags,
  retained `Surface::copy_page`, stream-to-direct-C oracle equality, stable
  marker checks, and PS/SVG negative PDF-metadata assertions.
- 2026-07-07: PNG path/stream parity now mirrors pycairo's filename versus
  file-object fixture: write the same ARGB32 image through `Surface::write_to_png`
  and `Surface::write_to_png_stream`, compare the exact PNG bytes, then read both
  forms back and compare copied pixel data.
- 2026-07-07: PNG stream parity now includes a public black-box data round
  trip: write an ARGB32 image through `Surface::write_to_png_stream`, read it
  back through `Surface::image_from_png_stream`, and compare copied pixel data
  with the original source image.
- 2026-07-07: PDF surface lifecycle parity now includes post-finish
  `SurfaceFinished` checks for page labels, metadata, custom metadata, and
  outline insertion, matching the corresponding pycairo post-context-manager
  error fixtures.
- 2026-07-07: PS surface lifecycle parity now includes post-finish
  `SurfaceFinished` checks for the EPS getter, page size, DSC setup, DSC page
  setup, and DSC comments, matching the same lifecycle policy used by PDF/SVG
  backend helpers.
- 2026-07-07: Vector stream callback parity now covers the pycairo
  closed-before-finish fixture by creating PDF/PS/SVG stream surfaces, closing
  the writer state after construction, and requiring finish-time
  `CairoIOError(WriteError, _)` mapping.

## Current Coverage Snapshot

| Area | Status | Evidence |
|---|---|---|
| MoonBit package setup | Done | `moon.mod` uses `source = "src"` with the public package in `src/moon.pkg`, public C glue compiled by the imported `src/native/moon.pkg` native-stub package, native Cairo compile/link flags refreshed from `pkg-config` by `scripts/configure-link-flags.sh`, pycairo-style split C stubs with `cairoon_misc.c` limited to module-level helpers, Surface C glue and raw FFI split by base/png/mime/state/font-options/image/mapped-image/recording/backend files, Pattern C glue and raw FFI split by base-gradient/mesh/raster-source-public/raster-source-callback-owner files, Font C glue and raw FFI split by font-options/font-face/scaled-font/text-to-glyphs/oracle files, Context C glue split by core/font-text/matrix/state/path/clip-extents/paint families, test-only oracle glue split into common/file/vector-entry/vector-scenes/vector-tag-scenes/vector-text-state/backend-combo/backend-tag-matrix/backend-text-state/backend-deep-tag/PDF-vector/PS-vector/SVG-vector/image/pattern-base/pattern-combo/pattern-stack/pattern-operator/pattern-special C files, Context MoonBit wrappers split by core/font-text/matrix/state/path/clip-paint families, Surface MoonBit wrappers split by core/image/png/mime/state/font-options/recording/tee families, Pattern MoonBit wrappers split by core/gradient/mesh/raster-source families, the internal version, format, status, PDF helper, PS helper, SVG helper, stream helper, and cstring helper implementation packages split out behind facade wrappers, vector-output white-box MoonBit helpers/tests split into common/scene-dispatch/scene-pattern/scene-tag/marker/oracle files, and stream-equivalence white-box helpers/tests split into helper/base-vector/backend-feature/tag-heavy/deep-tag files |
| Version helpers and module constants | Done | `cairo_version`, `cairo_version_string`, `CAIRO_VERSION*`, `HAS_*`, `MIME_TYPE_*`, tag constants, `PDF_OUTLINE_ROOT`, `FORMAT_INVALID`, and `COLOR_PALETTE_DEFAULT`; pycairo `test_version` parity covers runtime/compile-time version equality, `CAIRO_VERSION_STRING == major.minor.micro`, and Cairo's encoded integer formula; version externs and UTF-8 decoding live in `src/internal/version`, primitive generated constants live in `src/core/constants`, and public facade signatures/names are preserved |
| Status and error mapping | Done | `Status`, `CairoError`, `run_cairo`, exhaustive status-classification tests in `src/tests/status/core`, pycairo enum smoke coverage for `Jbig2GlobalMissing`, and real `Context::restore` `InvalidRestore` status/message payload propagation in `src/tests/context/state`; raw status-message lookup and UTF-8 decoding live in `src/internal/status`, while public enum constructors and `Status::message` stay in the facade; MoonBit intentionally splits invalid-argument statuses into `CairoInvalidArgument` |
| Matrix | Done | Pure value equivalent with field access, transform/translate/scale/rotate/multiply/invert/component tests, pycairo matrix equality/component/invert/multiply/translate/scale/rotate/transform fixtures, deterministic property tests for multiplication associativity, inverse identity, composed point/distance transforms, distance-vs-point-delta behavior, and executable reference docs |
| Image surface basics | Done | Create, similar-image create, map/unmap to image, buffer-backed create-for-data, PNG path load/save, PNG path-vs-stream byte equality, PNG stream read/write, MIME data, status, finish, flush, pycairo-derived surface context-manager/cmp-hash/content-format/device-scale/device-offset/fallback-resolution/dirty-page/create-for-rectangle/create-similar/create-similar-image fixtures, finished-status base-method/capability/dirty/state/error mapping, `get_data` after finish for ordinary, buffer-backed, and PNG-loaded image surfaces, width/height/stride/format including Cairo float image formats, pointer equality/hash for ordinary surfaces, copied data, empty 0x0 image `get_data`/copy-data behavior, compact pycairo-derived `create_for_data` prefilled ARGB32 array byte-copy fixture, pycairo-derived `create_for_data` retained-buffer release-on-finish fixture, width/height/stride/format/copy-data `SurfaceFinished` and `SurfaceTypeMismatch` error mapping, mutable `ImageData` views for image and mapped-image surfaces, retained `ImageData` views observing base-surface `SurfaceFinished`, targeted normal/ASan gate coverage through `src/tests/image/{properties,data,mapped,png,buffer}`, executable Surface reference docs, and forty-four-scene direct C Cairo ARGB32 oracle coverage for ordinary and buffer-backed image surfaces covering paint, stroke, fill/stroke rectangles, Bezier paths, transforms, RGBA compositing, linear/radial gradients, toy-font `text_path`, toy-font `show_text`, `glyph_path`, `show_glyphs`, `show_text_glyphs`, source-surface offset sampling, mask-surface offset compositing, raster-source pattern repeat rendering, dashed round-cap stroke state, clipped paint/fill output, `OperatorClear` compositing output, group compositing output, mask pattern compositing output, even-odd fill-rule output, surface-pattern `Reflect`/`Nearest`/`DitherBest`/matrix combination output, transformed repeated linear-gradient `Repeat`/`Nearest`/`DitherFast`/matrix output, transformed radial-gradient `Reflect`/`Nearest`/`DitherGood`/matrix output, surface-pattern `Pad`/`Bilinear`/`DitherDefault`/matrix output, surface-pattern `ExtendNone`/`Nearest`/`DitherNone`/matrix output, transformed linear-gradient `Pad`/`Bilinear`/`DitherBest`/matrix output, transformed radial-gradient `Repeat`/`Bilinear`/`DitherFast`/matrix output, surface-pattern `Repeat`/`Bilinear`/`DitherGood`/matrix output, transformed linear-gradient `ExtendNone`/`Good`/`DitherNone`/matrix output with duplicate-offset stops, transformed radial-gradient `Pad`/`Good`/`DitherDefault`/matrix output, curve/multi-patch mesh-pattern matrix output, nested surface/linear/radial/mask pattern-stack clip/save/restore/OperatorAtop output, mesh/linear surface-mask group-compositing output, set_source_surface/repeated-surface-pattern/radial-mask group-compositing output, surface-pattern device-offset mask with `OperatorScreen` group compositing output, surface/radial-gradient `OperatorOverlay` group-mask output, mesh/surface/linear `OperatorSoftLight` group-mask output, surface-pattern stroke `OperatorDifference` group-mask output, radial-gradient `OperatorColorDodge` over transformed reflected surface-pattern output, transformed repeated surface-pattern plus reflected linear-gradient `OperatorHslHue` group-mask output, and hairline stroke output |
| Device basics | Done | External object ownership, status/type/equal/hash, MoonBit `Eq`/`Hash` traits, finish/flush/acquire/release, scoped acquire and finish helpers, `Surface::get_device`, script file/stream devices including path-output readback, pycairo-derived context-manager, cmp/hash, image-surface `get_device`, script mode/comment, recording-replay, and script-surface target-paint parity fixtures, targeted normal/ASan gate coverage, and executable backend reference docs |
| Recording surface | Done | Constructor with optional extents, extents/ink-extents queries, subtype-mismatch checks, replay through surface patterns, targeted normal/ASan gate coverage, and executable backend reference docs |
| PDF surface | Partial | Filename/no-output/stream-writer constructors, supported versions and version strings with raw version helper externs in `src/internal/pdf`, pycairo-compatible raw C-int version string helpers, typed/raw version restriction, page size, typed/raw metadata, custom metadata, page labels, thumbnails, single-flag outline compatibility, typed outline flag sets, raw outline bitset compatibility, executable backend reference docs, stable structural output markers, direct C Cairo oracle comparison for thirty-four deterministic PDF 1.4 vector/tag/document-feature/page-operation/backend-combo scenes covering paint, stroke, fill/stroke rectangles, Bezier paths, transforms, linear/radial gradients, toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern, mask surface, mesh pattern, URI-link tag, named-destination tag, document-structure tag scenes, URI-link text tag, named-destination text tag, document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, one drawing-oriented two-page document-feature scene, one text/tag-aware two-page document-feature scene, one page-operation two-page document-feature scene, and backend feature/tag stream-combo through research-note scenes covering metadata, custom metadata, page labels, outlines, URI links, named destinations, document-structure tags, surface patterns, text, `show_text_glyphs`, Index/Code/Formula/Figure/Caption/BibEntry and table/list/note/reference structure tags, and mixed copy/show-page transitions, PDF metadata/custom-metadata/page-label/outline output markers, PDF page thumbnail output markers plus direct C oracle comparison, PDF multi-page output markers, PDF tagged multi-page text structure markers, PDF rectangle tag marker checks, PDF text tag marker checks, PDF direct document-feature marker checks, PDF tagged `show_text_glyphs` Link/structure marker checks, PDF grouped glyph/tag combined marker checks, PDF copy_page retained two-page marker checks, PDF Surface::copy_page retained two-page marker checks, PDF Surface::show_page cleared two-page marker checks, PDF mixed vector/tag/text combined tag markers, PDF layered three-page combined tag markers, PDF wide three-page combined tag markers, combined two-page PDF output markers covering metadata, custom metadata, page labels, outlines, URI links, named destinations, and document-structure tags, backend-combo PDF marker checks covering metadata/page labels/outlines/URI/dest/structure tags, PDF JPEG MIME data passthrough and PDF thumbnail output with direct C oracle comparison, subtype-mismatch checks for page-size/version/metadata/custom-metadata/page-label/thumbnail/outline helpers, stream callback WriteError and invalid-status fallback mapping, and stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, PDF text document-feature, PDF page-operation document-feature, PDF JPEG MIME and PDF thumbnail output, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend document-feature stream marker checks covering PDF/PS/SVG backend markers, backend feature/tag stream-combo output, and deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks; broader normalized PDF tag/metadata/multi-page output comparison remains |
| PS surface | Partial | Filename/no-output/stream-writer constructors, supported levels and level strings with raw level helper externs in `src/internal/ps`, pycairo-compatible raw C-int level string helpers, typed/raw level restriction, EPS mode, page size, DSC comments, executable backend reference docs, stable page/drawing output markers, direct C Cairo oracle comparison for seventeen deterministic vector scenes covering paint, stroke, fill/stroke rectangles, Bezier paths, transforms, linear/radial gradients, toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern, mask surface, and mesh pattern scenes with `CreationDate` normalization, plus normalized direct C oracle comparison for combined language-level/DSC setup/page-setup/two-page output, Link tag inertness, destination/document-structure tag scenes, URI-link/named-destination/document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, and one backend-feature/tag stream-combo scene covering DSC, inert tags, surface patterns, text, and `show_text_glyphs`, negative marker checks that destination/document-structure, tagged multi-page text, tagged `show_text_glyphs`, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, and backend-combo file/stream tag scenes do not emit PDF tag metadata, backend-combo PS DSC/page markers, subtype-mismatch checks for EPS/level/page-size/DSC helpers, stream callback output, WriteError, and invalid-status fallback mapping, and stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend document-feature stream marker checks covering PDF/PS/SVG backend markers, backend feature/tag stream-combo output, and deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks; broader normalized PS tag output comparison remains |
| SVG surface | Partial | Filename/no-output/stream-writer constructors, supported versions, version strings, pycairo-compatible raw C-int version string helpers, typed/raw version restriction, typed/raw document units, executable backend reference docs, stable geometry/color output markers, normalized direct C Cairo oracle comparison for seventeen deterministic vector scenes covering paint, stroke, fill/stroke rectangles, Bezier paths, transforms, linear/radial gradients, toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern with dynamic `source-*` id normalization, mask surface, and mesh pattern scenes, plus normalized direct C oracle comparison for combined version-restricted/document-unit/two-page output, Link tag inertness, destination/document-structure tag scenes, URI-link/named-destination/document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, and one backend-feature/tag stream-combo scene covering SVG version/unit settings, inert tags, surface patterns, text, and `show_text_glyphs`, negative marker checks that destination/document-structure, tagged multi-page text, tagged `show_text_glyphs`, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, and backend-combo file/stream tag scenes do not emit PDF tag metadata, backend-combo SVG unit/page markers, subtype-mismatch checks for document-unit/version helpers, finished-status coverage for document-unit getters/setters and version restriction, stream callback output, WriteError, and invalid-status fallback mapping, and stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend document-feature stream marker checks covering PDF/PS/SVG backend markers, backend feature/tag stream-combo output, and deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks; broader normalized SVG tag combinations remain |
| Context basics | Partial | Creation, status, pointer equality/hash, MoonBit `Eq`/`Hash` traits, pycairo `test_cmp_hash` same-target/different-context fixture, save/restore, CTM transforms, drawing state including hairline mode, pycairo default/enum/hairline drawing-state fixtures, raw operator C-int limit fixture, save/restore state restoration fixture, and pycairo raw C-int operator/antialias/fill-rule/line-cap/line-join/content passthrough compatibility, and direct C even-odd fill-rule and hairline stroke output, clip APIs including pycairo empty-path clip/reset `in_clip` behavior, pycairo's 42x42 default clip-extents fixture, and explicit `clip`/`clip_preserve` path clear/preserve lifecycle coverage, current-point fixture, absolute move/line/curve current-point fixtures, rectangle/relative-path current-point fixtures, dash-offset normalization fixture, matrix setter/translate/scale/transform/rotate fixtures, scalar line-width/miter-limit/tolerance setter fixtures, source RGBA round-trip fixture, and explicit source-pattern round-trip fixture, extents and hit-testing including pycairo polygon fill-extents, polygon path-extents, line in-stroke, empty in-fill, and empty stroke-extents coverage, group APIs including pycairo group stack fixture, group-target stack restoration and direct C ARGB32 group compositing plus mask pattern compositing output, tag APIs including pycairo `test_tags` begin/end coverage, path primitives including pycairo copied `append_path` string fixture, rectangle path-extents coverage and zero-radius `arc`/`arc_negative` non-empty path behavior, source colors/surfaces including pycairo mask/mask-surface/alpha-paint fixtures, text/font/glyph APIs including context/scaled-font font-extents parity, pycairo text/glyph method fixtures and PDF show_text_glyphs tuple fixture, font-face default/reset/custom round-trip fixtures, font-options round-trip fixture, set_font_matrix identity fixture, scaled-font round-trip fixture, and ScaledFont returned face/options lifetime, pycairo simple smoke coverage for `clip_preserve`, `copy_page`, `copy_path_flat`, `fill`, `fill_preserve`, `font_extents`, `identity_matrix`, `new_sub_path`, `show_page`, `stroke_preserve`, `get_dash_count`, `get_font_matrix`, `get_group_target`, scalar getters, and final status, paint/fill/stroke, targeted ASan coverage for state/matrix/path/group/text/glyph/extents/clip/painting wrappers, font-options/font-face/scaled-font wrappers, and external-object lifetime stress, plus executable reference docs; broader method-by-method pycairo context parity remains tracked by the detailed rows |
| Pattern basics | Partial | Solid/surface/raster-source/mesh pattern constructors, status, pointer equality/hash, MoonBit `Eq`/`Hash` traits, pycairo `test_cmp_hash` repeated-`get_source()` equality/hash fixture, pycairo independent solid-pattern identity/hash fixture, source setting, RGBA, extend/filter/dither/matrix state including pycairo solid-pattern default extend/filter/dither/matrix/clamped-RGBA fixtures, pycairo surface-pattern default extend/filter/dither/matrix/get-surface fixture, pycairo setter round-trip fixtures, pycairo gradient point/color-stop/radial-circle fixtures, pycairo mesh example patch fixture, pycairo mesh lifecycle error-state fixture, pycairo raster-source callback state/acquire-release paint fixtures, and pycairo raw C-int extend/filter/dither passthrough compatibility, gradient state including duplicate-offset color-stop insertion order and snapshot coverage, targeted ASan coverage for core/pycairo-parity, gradient, and mesh wrappers, raster-source acquire/get-acquire callback APIs with acquire-only retained-owner cleanup, compatible target/extents acquire coverage, failure/recovery coverage, deterministic callback replacement/failure fuzz, acquire-only retained-owner replacement fuzz, stale release-to-acquire-only replacement coverage, failed acquire replacement owner-count fuzz, four-seed 72-step-per-seed callback state-machine fuzz, multi-seed manual get-callback acquire/release fuzz, raster-source callback allocation stress, mesh patch lifecycle/query APIs including pycairo's curve-first patch case, referenced surface return, split black-box tests for core/surface vs raster-source behavior, raster-source core/fuzz/state test files, and executable reference docs, plus direct C ARGB32 oracle output coverage for a surface-pattern `Reflect`/`Nearest`/`DitherBest`/matrix combination, a transformed repeated linear-gradient `Repeat`/`Nearest`/`DitherFast`/matrix combination, a transformed radial-gradient `Reflect`/`Nearest`/`DitherGood`/matrix combination, surface-pattern `Pad`/`Bilinear`/`DitherDefault`/matrix, `ExtendNone`/`Nearest`/`DitherNone`/matrix, and `Repeat`/`Bilinear`/`DitherGood`/matrix combinations, transformed linear-gradient `Pad`/`Bilinear`/`DitherBest`/matrix and `ExtendNone`/`Good`/`DitherNone`/matrix output with duplicate-offset stops, transformed radial-gradient `Repeat`/`Bilinear`/`DitherFast`/matrix and `Pad`/`Good`/`DitherDefault`/matrix output, mesh-pattern fill plus curve/multi-patch matrix output, nested surface/linear/radial/mask pattern-stack clip/save/restore/OperatorAtop output, mesh/linear surface-mask group-compositing output, set_source_surface/repeated-surface-pattern/radial-mask group-compositing output, surface-pattern device-offset mask with `OperatorScreen` group-compositing output, surface/radial-gradient `OperatorOverlay` group-mask output, and mesh/surface/linear `OperatorSoftLight` group-mask output, surface-pattern stroke `OperatorDifference` group-mask output, and radial-gradient `OperatorColorDodge` over transformed reflected surface-pattern output, and transformed repeated surface-pattern plus reflected linear-gradient `OperatorHslHue` group-mask output; broader pattern combination parity remains for unenumerated paths |
| Font faces | Done | Base external object, toy constructor/getters, pycairo-compatible raw C-int toy slant/weight parsing with `InvalidSlant`/`InvalidWeight` mapping for invalid raw values, Context get/set/select, borrowed-return references, targeted normal/ASan gate coverage, and executable Font reference docs |
| Font options | Done | External object, copy/merge/equal/hash, MoonBit `Eq`/`Hash` traits, all portable state fields including pycairo-compatible raw C-int antialias/subpixel/hint-style/hint-metrics/color-mode passthrough, Surface/Context accessors, targeted normal/ASan gate coverage, and executable Font reference docs |
| Scaled fonts | Done | External object, constructor, matrices/options/font-face getters, returned font-face/options lifetime after the source scaled-font scope exits, sheared font/CTM scale-matrix combination coverage, font/text/glyph extents including embedded-NUL validation, empty, single/multi/spaced ASCII, precomposed/decomposed Latin, CJK, Arabic RTL, and emoji UTF-8 text-to-glyph string/coordinate cases with direct C Cairo oracle coverage, targeted normal/ASan gate coverage, executable Font reference docs for matrices/options/metrics/text-to-glyphs/errors, and Context get/set |
| Docs | Done | README smoke example, AGENTS spec, `PROJECT_LAYOUT.md` package-management plan plus executable root-layout guard, executable Matrix/Surface/Backend Surface/Context/Font/Path/Pattern/Region reference notes including Context drawing-state, FontOptions, Pattern, FontFace, and backend raw enum compatibility, Surface and Device scoped lifecycle examples, `TESTING.md` reliability gates and sufficiency scorecard, `PACKAGING.md` release/CI rules, and `PORTING_FROM_PYCAIRO.md` migration notes are in place for the current migrated surface. Keep this row current whenever new public APIs land |
| Tests | Partial | Native and ASan test runs cover the current migrated slices only; `scripts/verify.sh` runs the local reliability gate (`moon fmt --check`, `scripts/configure-link-flags.sh --check`, native check/full tests, key white-box oracles, `moon info --target native`, and targeted ASan when clang is available); vector backends have stable structural output marker checks plus direct C Cairo oracle comparisons for seventeen deterministic PDF/PS/SVG scenes including `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern, mask surface, and mesh pattern scenes, with SVG dynamic `source-*` id normalization, plus three PDF-only direct C Cairo tag-oracle scenes covering URI links, named destinations, and document-structure tags, three PDF-only direct C Cairo text-tag oracle scenes covering URI-link, named-destination, and document-structure text tags, PS/SVG Link tag inertness direct C oracle scenes, PS/SVG destination and document-structure tag direct C oracle scenes plus negative PDF-metadata marker checks for those scenes, PS/SVG URI-link/named-destination/document-structure text tag direct C oracle scenes plus negative PDF-metadata marker checks for the URI-link/destination/document-structure text scenes, a cross-backend tagged multi-page text direct C oracle scene, a cross-backend tagged `show_text_glyphs` direct C oracle scene, a cross-backend grouped glyph/tag multi-page direct C oracle scene, a cross-backend copy_page retained two-page direct C oracle scene, a cross-backend Surface::copy_page/Surface::show_page retained two-page direct C oracle, a cross-backend Surface::show_page cleared two-page direct C oracle, a cross-backend mixed vector/tag/text direct C oracle scene, a cross-backend layered three-page clip/dash/surface-pattern/mask/tag/text direct C oracle scene, a cross-backend wide three-page URI/destination/document-structure tag/vector direct C oracle scene, a cross-backend backend-feature/tag stream-combo direct C oracle scene, and a cross-backend deep tag tree direct C oracle scene, PDF tagged multi-page text structure marker checks, PDF rectangle tag marker checks, PDF text tag marker checks, PDF direct document-feature marker checks, PDF tagged `show_text_glyphs` Link/structure marker checks, PDF grouped glyph/tag combined marker checks, PDF copy_page retained two-page marker checks, PDF Surface::copy_page retained two-page marker checks, PDF Surface::show_page cleared two-page marker checks, PDF mixed vector/tag/text combined tag marker checks, PDF layered three-page combined tag marker checks, PDF wide three-page combined tag marker checks, backend-combo PDF/PS/SVG marker checks plus PS/SVG file/stream negative tag-metadata marker checks, PS/SVG tagged multi-page negative PDF-metadata marker checks, PS/SVG tagged `show_text_glyphs` negative PDF-metadata marker checks, PS/SVG grouped glyph/tag negative PDF-metadata marker checks, PS/SVG mixed vector/tag/text negative PDF-metadata marker checks, PS/SVG layered three-page negative PDF-metadata marker checks, PS/SVG wide three-page negative PDF-metadata marker checks, three PDF-only document-feature/page-operation oracle scenes covering metadata, custom metadata, page labels, outlines, URI links, named destinations, and document-structure tags across two pages, including one text/tag-aware combined scene, one PS-only DSC/multi-page oracle scene, and one SVG-only version/unit/multi-page oracle scene, PDF metadata/custom-metadata/page-label/outline output markers, PDF page thumbnail output markers plus direct C oracle comparison, PDF/PS/SVG multi-page output markers, a combined two-page PDF marker test covering metadata, custom metadata, page labels, outlines, URI links, named destinations, and document-structure tags, combined PS language-level/DSC setup/page-setup/multi-page output markers, combined SVG version-restricted/document-unit/multi-page output markers, PDF JPEG MIME data passthrough and PDF thumbnail output with direct C oracle comparison, PS/SVG link tags have inert-output checks, PDF/PS/SVG backend methods cover subtype-mismatch errors including extended PDF custom-metadata/thumbnail/outline helpers, PS size/setup/page-setup helpers, and SVG document-unit finished getters, PDF/PS/SVG, PNG, and script stream callbacks have output/error coverage, including callback-returned chunk retention after user callbacks return, PDF/PS/SVG, PNG-writer, and script-writer invalid-status fallback to `WriteError`, PDF/PS/SVG stream-vs-file normalized equality covers deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font `text_path`/`show_text` vector scenes, `glyph_path`/`show_glyphs` vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, PDF text document-feature, PDF JPEG MIME and PDF thumbnail output, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend document-feature stream marker checks covering PDF/PS/SVG backend markers, backend feature/tag stream-combo output, and backend deep tag tree stream-to-direct-C normalized oracle comparison, PNG stream reading has image round-trip and short-read coverage, image/mapped-image data has mutable-view lifetime, unmap, scoped finish/unmap success/error helpers, base-surface finish invalidation, and 1000-iteration allocation stress coverage, backend stream callback stress covers PDF/PS/SVG stream surfaces, PNG stream write/read, script stream devices, scoped Device finish, and stream `WriteError` paths, raster-source callback stress covers set/get/manual acquire/release/replace/clear paths plus release-only callback state, acquire-only retained-owner release-trampoline cleanup, 64-step acquire-only retained-owner replacement fuzz, stale release-to-acquire-only replacement coverage, failed acquire replacement owner-count fuzz, finished-surface acquire failure injection, post-failure acquire replacement recovery, compatible target/extents acquire rendering with device-offset source surfaces, deterministic callback replacement/failure fuzz with dynamic compatible source surfaces, deterministic four-seed 72-step-per-seed callback state-machine fuzz covering clear/release-only/acquire-only/acquire+release/failure/dynamic-source transitions, multi-seed manual get-callback acquire/release fuzz covering release-only, acquire-only, acquire+release, failed-source, dynamic compatible-source, pixel readback, and owner-count balance paths, and direct C ARGB32 oracle repeat rendering, retained-owner stress covers owner graph cases, finalizer graph multi-seed fuzz covers surface/context/pattern/font/path/region/mapped-image/raster-source/script-device owner graphs, explicit and implicit mapped-image cleanup edges, expected Cairo errors, callback clearing, and allocation pressure, Context path tests cover pycairo rectangle path-extents behavior, and Context pycairo parity tests cover 42x42 default clip extents, zero-radius arc/arc_negative non-empty paths, polygon path_extents, polygon fill_extents, empty in_fill, line in_stroke, rectangle path_extents, relative path current-point updates, empty stroke_extents, default coordinate conversions, matrix setter/translate/scale/transform/rotate fixtures, current-point state, dash-offset normalization, drawing-state default getters and enum setter round trips, raw operator C-int limit passthrough, save/restore drawing-state restoration, hairline getter/setter, scalar line-width/miter-limit/tolerance setters, source RGBA round trips, explicit source-pattern round trips, copied `append_path` strings, empty clip/reset `in_clip` behavior, group stack push/pop variants, mask/mask-surface/alpha-paint pixel fixtures, font-face default/reset/custom round trips, set_font_matrix identity, font-options round trip, scaled-font round trip, empty font-family selection, set_font_size font-matrix fixtures, text/font-extents/show-text/text-path fixtures, glyph-extents/show-glyphs/glyph-path fixtures, PDF show_text_glyphs tuple fixture, and pycairo `test_simple` method-smoke flow covering `clip_preserve`, `copy_page`, `copy_path_flat`, `fill`, `fill_preserve`, `font_extents`, `identity_matrix`, `new_sub_path`, `show_page`, `stroke_preserve`, dash count, font matrix, group target, scalar getters, and final status; both are now part of the targeted ASan gate, Context lifetime tests cover `get_target`, `get_group_target`, `get_source`, and PDF/PS stream target wrappers after source/context/surface scopes exit and are now part of the targeted ASan gate, surface/device/object-trait tests cover base surface state, retained `ImageData` views, subsurface parent retention, recording replay, MIME data/support, PDF/PS/SVG helper errors, script devices, script stream callbacks, script surfaces, and `Eq`/`Hash` for `Surface`, `Context`, `Pattern`, `Device`, `FontOptions`, `FontFace`, `ScaledFont`, plus `Eq`/`Compare` for `Path` and `Eq` for `Region`, and are now part of the targeted normal/ASan gate, surface similar/subsurface constructors have invalid-size suberror coverage and pycairo numeric-limit success paths, image/PDF/PS/SVG surfaces cover backend-specific MIME support matrices, image surfaces and buffer-backed image surfaces cover Cairo float image formats `Rgb96F` and `Rgba128F`, ordinary image surface finish/status behavior maps base methods, capability checks, dirty markers, state queries/setters, `get_data`, and retained `ImageData` views to `SurfaceFinished`, `Format::stride_for_width` covers legacy, 16-bit, 30-bit, float, and invalid-width cases, `Glyph` preserves Cairo's unsigned-long index width through a `UInt64` value field and FFI marshal arrays, pure value types cover pycairo numeric limits for `Rectangle`, `RectangleInt`, `TextExtents`, and `TextCluster`, and Matrix deterministic property tests cover multiplication associativity, inverse identity, composed point/distance transforms, and distance-vs-point-delta behavior, UTF-8 text input tests cover toy font family, context font-family, context text, show-text-glyphs, and ScaledFont text APIs with embedded-NUL rejection, region rectangle boolean coverage includes `xor_rectangle` split semantics and pycairo-style chainable mutator returns, executable Matrix reference docs cover component/transform/multiply/invert/error examples, executable Surface reference docs cover scoped finish/unmap, image properties, buffer-backed data, similar/subsurface constructors, mapped images, PNG/MIME helpers, and checked surface errors, executable Backend Surface reference docs cover PDF/PS/SVG streams and settings, PDF metadata/outlines, PS DSC, SVG units, recording replay, tee fanout, script devices, scoped Device finish, script surfaces, and checked backend errors, executable Context reference docs cover construction/state, transforms, paths, fill/stroke output, clipping, groups, text/glyphs, and checked context errors, executable Font reference docs cover FontOptions state/copy/merge, color palettes, toy faces, Surface/Context font options, ScaledFont matrices/metrics, text-to-glyphs, and checked font errors, executable Path reference docs cover typed segment/string/flatten/append/error examples, executable Pattern reference docs cover solid/surface/gradient/mesh/raster-source/error examples, executable Region reference docs cover construction/query/boolean/chainable-mutator examples, external value-wrapper stress covers `Path`, `Region`, `FontOptions`, `FontFace`, `ScaledFont`, solid/gradient/mesh `Pattern`, recording/similar/Tee `Surface`, and script stream `Device`, ScaledFont font/text/glyph extents, sheared matrix composition, and text-to-glyph empty, single/multi/spaced ASCII, precomposed/decomposed Latin, CJK, Arabic RTL, and emoji UTF-8 cases have direct C Cairo/public API comparison, and ordinary plus buffer-backed image rendering have forty-four-scene direct C Cairo ARGB32 oracle comparison including toy-font `text_path`, toy-font `show_text`, `glyph_path`, `show_glyphs`, `show_text_glyphs`, source-surface offset sampling, mask-surface offset compositing, raster-source pattern repeat rendering, dashed round-cap stroke state, clipped paint/fill output, `OperatorClear` compositing output, group compositing output, mask pattern compositing output, even-odd fill-rule output, surface-pattern `Reflect`/`Nearest`/`DitherBest`/matrix combination output, transformed repeated linear-gradient `Repeat`/`Nearest`/`DitherFast`/matrix output, transformed radial-gradient `Reflect`/`Nearest`/`DitherGood`/matrix output, surface-pattern `Pad`/`Bilinear`/`DitherDefault`/matrix output, surface-pattern `ExtendNone`/`Nearest`/`DitherNone`/matrix output, transformed linear-gradient `Pad`/`Bilinear`/`DitherBest`/matrix output, transformed radial-gradient `Repeat`/`Bilinear`/`DitherFast`/matrix output, surface-pattern `Repeat`/`Bilinear`/`DitherGood`/matrix output, transformed linear-gradient `ExtendNone`/`Good`/`DitherNone`/matrix output with duplicate-offset stops, transformed radial-gradient `Pad`/`Good`/`DitherDefault`/matrix output, and curve/multi-patch mesh-pattern matrix output, nested surface/linear/radial/mask pattern-stack clip/save/restore/OperatorAtop output, mesh/linear surface-mask group-compositing output, and set_source_surface/repeated-surface-pattern/radial-mask group-compositing output, surface-pattern device-offset mask with `OperatorScreen` group compositing output, surface/radial-gradient `OperatorOverlay` group-mask output, mesh/surface/linear `OperatorSoftLight` group-mask output, surface-pattern stroke `OperatorDifference` group-mask output, radial-gradient `OperatorColorDodge` over transformed reflected surface-pattern output, transformed repeated surface-pattern plus reflected linear-gradient `OperatorHslHue` group-mask output, and hairline stroke output, but broader tag/metadata/multi-page normalized differential comparison remains |

## Recent Reliability Slices

| Slice | Status | Notes |
|---|---|---|
| Warning-free native verify gate | Done | Removes reserved-keyword `extend` parameter names from Pattern public/raw FFI wrappers without changing generated public signatures, and upgrades `scripts/verify.sh` so native check, support-package tests, extracted black-box/oracle tests, the full native suite, and targeted clang/ASan test-package runs use `--deny-warn`; the GitHub failure at `e644b9a` was an older PDF tag stream equality oracle, already stabilized locally by PDF marker-based stream tests and PS/SVG-only byte equivalence |
| Top-level and portable method inventory parity | Done | Extends `scripts/check-api-inventory.py` in `scripts/verify.sh`; the script parses parent `cairo/__init__.pyi`, requires every public top-level pycairo class/function to have an implementation or explicit product-decision anchor in this inventory, and requires every portable pycairo class method in the first-product scope to have a public MoonBit API anchor in `src/pkg.generated.mbti` |
| Black-box test package extraction probe | Done | Moves version-helper tests into `src/tests/api`, proving external test packages can import the stable `CAIMEOX/cairoon` public package while preserving `moon.mod source = "src"`; `scripts/check-project-layout.py` rejects root-level MoonBit test packages and requires Cairo `cc-link-flags` for external cairoon test packages |
| Core API black-box package expansion | Done | Moves enum and pycairo `test_api.py` smoke/lifetime tests into `src/tests/api`, proving public constructors, constants, methods, and smoke/lifetime behavior work from an external MoonBit package |
| API family package split | Done | Splits API tests into `src/tests/api/{version,enums,pycairo}`, leaving `src/tests/api` as a pure directory container while preserving the 1 version, 4 enum/format, and 8 pycairo `test_api.py` fixtures through separately linked published-API black-box packages |
| Status black-box package extraction | Done | Moves `check_status`, `run_cairo`, `Status::message`, and exhaustive `CairoError` suberror classification tests into `src/tests/status`, proving checked-error mapping works from an external MoonBit package while keeping `Status` and `CairoError` constructors in the public facade |
| Status package split | Done | Moves status/error tests into `src/tests/status/core`, leaving `src/tests/status` as a pure directory container while preserving the 2 checked-error mapping fixtures through a separately linked published-API black-box package |
| Value black-box package extraction | Done | Moves Rectangle, RectangleInt, Glyph, TextCluster, TextExtents, FontExtents, component, positive/negative invalid-index, hash/equality, numeric-limit, clip-rectangle, recording-extents, and Context/ScaledFont extents-return tests into `src/tests/value`, proving pure value behavior works through the public package from an external MoonBit test package |
| Value family package split | Done | Splits value tests into `src/tests/value/{core,pycairo}`, leaving `src/tests/value` as a pure directory container while preserving the 9 pure value and 7 pycairo value parity fixtures through separately linked published-API black-box packages |
| Matrix black-box package extraction | Done | Moves Matrix value and property tests into `src/tests/matrix`, proving imported public types, methods, checked errors, and suberror pattern matching work from an external MoonBit test package; `scripts/verify.sh` now discovers every `src/tests/**/moon.pkg` package automatically for normal and ASan targeted gates |
| Matrix family package split | Done | Splits Matrix tests into `src/tests/matrix/{core,property,pycairo}`, leaving `src/tests/matrix` as a pure directory container while preserving the 5 core Matrix, 4 deterministic property, and 4 pycairo `test_matrix.py` parity fixtures through separately linked published-API black-box packages |
| Region black-box package extraction | Done | Moves Region tests into `src/tests/region`, proving Region constructors, copies, containment/overlap queries, mutating boolean operations, pycairo-style chaining, and invalid-index error mapping work through the public package from an external MoonBit test package |
| Region family package split | Done | Splits Region tests into `src/tests/region/{core,pycairo}`, leaving `src/tests/region` as a pure directory container while preserving the 10 core Region and 5 pycairo `test_region.py` parity fixtures through separately linked published-API black-box packages |
| Surface black-box package extraction | Done | Moves base surface, similar/subsurface, MIME, and pycairo base-surface tests under `src/tests/surface`, proving core surface behavior works through the public package from external MoonBit test packages |
| Surface family package split | Done | Splits surface tests into `src/tests/surface/{base,mime,pycairo,subsurface}`, leaving `src/tests/surface` as a pure directory container while proving base state/status, MIME storage/support, pycairo surface parity, and subsurface behavior still link and run through separately linked external MoonBit test packages |
| Image surface black-box package extraction | Done | Moves ImageSurface, ImageData, MappedImageSurface, PNG path, buffer-backed, float-format, and raw image-format tests under `src/tests/image`, proving image-family behavior works through the public package from external MoonBit test packages |
| Image family package split | Done | Splits image tests into `src/tests/image/{properties,data,mapped,png,buffer}`, leaving `src/tests/image` as a pure directory container while proving image properties/raw formats, ImageData, mapped-image lifecycle, PNG path helpers, and buffer-backed image surfaces still link and run through separately linked external MoonBit test packages |
| Backend surface black-box package extraction | Done | Moves Recording, PDF, PS, SVG, and Tee surface tests under `src/tests/backend`, proving backend constructors, raw backend enums, subtype/finished errors, filename constructors, replay/fanout behavior, and backend helper APIs work through the public package from external MoonBit test packages |
| Backend surface family package split | Done | Splits backend surface tests into `src/tests/backend/{pdf,ps,recording,svg,tee}`, leaving `src/tests/backend` as a pure directory container while proving each backend family still links and runs through a separately linked external MoonBit test package |
| Context black-box package extraction | Done | Moves shared pycairo parity tests under `src/tests/context`, state-focused drawing-state/error tests into `src/tests/context/state`, path/clip/paint focused tests into `src/tests/context/{path,clip,paint}`, and group/matrix/extents/tag/text/glyph/text-to-glyphs tests into nested context packages, proving drawing-state, transform, path, pycairo parity, group, text/glyph, extents, clip, paint, tag, and ScaledFont text-to-glyph behavior works through the public package from external MoonBit test packages |
| Context state black-box package split | Done | Moves `context_state_test.mbt` into nested package `src/tests/context/state`, proving default drawing state, typed/raw drawing-state enum round trips, save/restore, dash clearing/invalid dash, invalid restore, and context-error propagation still work through the public package from a separately linked external MoonBit test package |
| Context path/clip/paint package split | Done | Moves `context_path_test.mbt`, `context_clip_test.mbt`, and `context_painting_test.mbt` into nested packages under `src/tests/context/{path,clip,paint}`, proving path construction/copy/error handling, clipping/clip-list behavior, and painting/source/page operations still work through separately linked external MoonBit test packages |
| Context support package split | Done | Moves group, matrix, extents, tag, text, glyph, and text-to-glyphs tests into nested packages under `src/tests/context/`, proving each moved behavior still works through separately linked external MoonBit test packages |
| Context pycairo package split | Done | Moves the remaining shared pycairo parity tests into `src/tests/context/pycairo`, leaving `src/tests/context` as a pure directory container while preserving the 36 shared Context parity fixtures through a separately linked published-API black-box package |
| Pattern black-box package extraction | Done | Moves pattern, gradient, mesh, raster-source, raster-source state, and raster-source fuzz tests under `src/tests/pattern`, proving pattern state, pycairo parity, callback APIs, and raster-source fuzz behavior work through the public package from external MoonBit test packages |
| Pattern gradient/raster package split | Done | Splits gradient tests into `src/tests/pattern/gradient` and raster-source tests into `src/tests/pattern/raster`, proving the moved behavior through separately linked external MoonBit test packages |
| Pattern family package split | Done | Splits the remaining Pattern root tests into `src/tests/pattern/{core,mesh,pycairo}`, leaving `src/tests/pattern` as a pure directory container while preserving the 28 core/surface/mesh/pycairo parity fixtures through separately linked published-API black-box packages |
| Font black-box package extraction | Done | Moves FontFace, FontOptions, and ScaledFont black-box tests under `src/tests/font/{face,options,scaled}`, proving font family/options/scaled-font behavior, context font round trips, invalid string handling, matrix queries, and text extents work through the public package from external MoonBit test packages while ScaledFont C-oracle tests stay white-box |
| Font pycairo parity fixtures | Done | Adds `src/tests/font/pycairo/font_pycairo_parity_test.mbt` with pycairo `test_font.py`-derived black-box fixtures for surface font-options default getters, FontOptions copy/equality/merge, antialias/hint-metrics/hint-style/subpixel-order setter behavior, color-mode getter/setter behavior, color-palette default/setter behavior, variation set/clear behavior, custom-palette color tuple values plus missing-index `InvalidIndex` mapping, FontFace and ScaledFont identity/hash behavior, toy-font family/slant/weight getters including Cairo's empty-family `Helvetica` normalization, ScaledFont extents, text-extents and glyph-extents value behavior, CTM/font/scale matrix getters, font-face/font-options getters, and empty/single/spaced ASCII `text_to_glyphs` result shape |
| Font family package split | Done | Splits font tests into `src/tests/font/{face,options,scaled,pycairo}`, leaving `src/tests/font` as a pure directory container while proving each font family still links and runs through a separately linked external MoonBit test package |
| Stream/device black-box package extraction | Done | Moves public stream and script device tests under `src/tests/stream`, proving PDF/PS/SVG/PNG stream callbacks, chunk retention, PNG reader errors, script devices, script surfaces, raw script content, and device lifetime helpers work through the public package from external MoonBit test packages while backend stream oracle tests stay white-box |
| Stream device package split | Done | Splits script device and script surface tests into `src/tests/stream/device`, proving the device stream behavior through a separately linked published-API package |
| Stream surface package split | Done | Moves the remaining root stream callback tests into `src/tests/stream/surface`, leaving `src/tests/stream` as a pure directory container while preserving the 14 PDF/PS/SVG/PNG stream writer/reader, PDF tag marker, and retained-chunk fixtures through a separately linked published-API black-box package |
| PNG path/stream pycairo parity | Done | Adds a public black-box fixture mirroring pycairo's filename-versus-file-object PNG test: `Surface::write_to_png` writes a temporary path, `Surface::write_to_png_stream` captures callback chunks, exact PNG bytes match, and both readback paths produce identical copied image data |
| Path black-box package extraction | Done | Moves Path tests into `src/tests/path`, proving path formatting/iteration/flattening/scope survival and Eq/Hash/Compare semantics for `Path` work through the public package from an external MoonBit test package |
| Path family package split | Done | Splits Path tests into `src/tests/path/{core,pycairo}`, leaving `src/tests/path` as a pure directory container while preserving the 7 core Path fixtures and 4 pycairo `test_path.py` parity fixtures through separately linked published-API black-box packages |
| Object trait black-box package extraction | Done | Keeps external object trait tests in `src/tests/object`, proving Eq/Hash semantics for Surface, Context, Pattern, Device, FontOptions, FontFace, ScaledFont, Region, and Path wrappers work through the public package from an external MoonBit test package |
| Object trait package split | Done | Moves object trait tests into `src/tests/object/core`, leaving `src/tests/object` as a pure directory container while preserving the 3 Eq/Hash/Compare external-object fixtures through a separately linked published-API black-box package |
| Lifetime/stress black-box package extraction | Done | Moves context lifetime, owner graph, value wrapper, image data, raster-source lifetime, stream callback stress, and finalizer fuzz tests under `src/tests/lifetime`, proving lifetime behavior through the public package from external MoonBit test packages |
| Lifetime finalizer package split | Done | Splits the multi-seed finalizer/error-exit graph fuzz test into `src/tests/lifetime/finalizer`, proving the finalizer fuzz target links and runs as its own published-API black-box package |
| Lifetime stream package split | Done | Splits backend stream callback allocation stress, multi-seed stream fuzz, and stream lifetime helpers into `src/tests/lifetime/stream`, proving stream callback lifetime coverage links and runs as a separately linked published-API black-box package |
| Lifetime stress package split | Done | Splits image-data view stress, external value-wrapper stress, and raster-source callback stress into `src/tests/lifetime/{image_data,value,raster}`, proving each lifetime stress family links and runs as a separately linked published-API black-box package |
| Lifetime root package split | Done | Splits context borrowed-return lifetime tests into `src/tests/lifetime/context` and retained owner-graph stress into `src/tests/lifetime/owner`, leaving `src/tests/lifetime` as a pure directory container while proving both remaining lifetime families link and run through separately linked published-API black-box packages |
| Native stub package extraction | Done | Moves public C glue ownership into `src/native/moon.pkg`, imports `CAIMEOX/cairoon/native` from the public package for linking, removes `native-stub` entries from `src/moon.pkg`, and strengthens the layout gate so every `src/native/*.c` is explicitly owned by `src/native/moon.pkg` |
| Constants oracle package extraction | Done | Moves compile-time cairo constant oracle tests into `src/tests/oracle/constants`, with local test-only extern declarations that link against the public package native stubs while asserting public constants through the published package seam |
| ScaledFont oracle package extraction | Done | Moves ScaledFont direct C extents and text-to-glyphs oracle tests into `src/tests/oracle/scaled_font`, with local test-only extern declarations and native result decoding while keeping public `TextGlyphRun` assertions through the published package seam |
| Image oracle package extraction | Done | Moves ARGB32 direct C scene oracle tests into `src/tests/oracle/image`, with a local test-only scene extern while rendering ordinary and buffer-backed image surfaces entirely through the published package seam |
| Image pattern overlay oracle | Done | Extends the direct C ARGB32 image oracle to forty scenes, adding a clipped group-mask composition that overlays a transformed reflected surface pattern with a repeated radial gradient through `OperatorOverlay`, then masks the group through a transformed linear pattern for both ordinary and buffer-backed image surfaces |
| Image pattern soft-light oracle | Done | Extends the direct C ARGB32 image oracle to forty-one scenes, adding a clipped group-mask composition that combines a mesh pattern, transformed surface pattern, repeated linear gradient, radial mask, and `OperatorSoftLight` for both ordinary and buffer-backed image surfaces |
| Image pattern difference-stroke oracle | Done | Extends the direct C ARGB32 image oracle to forty-two scenes, adding a clipped group-mask composition that paints a transformed linear pattern, differences a dashed transformed repeated surface-pattern stroke through `OperatorDifference`, overlays a translucent fill, and masks the group through a transformed radial pattern for both ordinary and buffer-backed image surfaces |
| Image pattern color-dodge oracle | Done | Extends the direct C ARGB32 image oracle to forty-three scenes, adding a separate pattern-operator scene that paints a transformed reflected surface pattern, then fills a transformed radial gradient through `OperatorColorDodge` for both ordinary and buffer-backed image surfaces |
| Image pattern HSL hue group-mask oracle | Done | Extends the direct C ARGB32 image oracle to forty-four scenes, adding a separate pattern-operator scene that paints a transformed repeated surface pattern, fills a transformed reflected linear gradient through `OperatorHslHue` inside a group, and masks the group through a transformed radial pattern for both ordinary and buffer-backed image surfaces |
| Image create-for-data array parity | Done | Adds a compact pycairo-derived black-box fixture for `Surface::image_for_data` default stride and prefilled ARGB32 array readback, proving an external MoonBit buffer can be wrapped and copied back byte-for-byte through the public package seam |
| Raster-source pattern oracle package extraction | Done | Moves raster-source owner-count, callback-state, and manual callback lifecycle white-box tests into `src/tests/oracle/pattern_raster`, with a test-only local owner-count extern while all raster-source operations use the published package seam |
| Vector/backend oracle package extraction | Done | Moves vector-output and backend stream white-box/oracle tests into `src/tests/oracle/vector_backend`, with local file/vector/direct-C oracle externs while all PDF/PS/SVG rendering and stream behavior is exercised through the published package seam |
| Oracle native package extraction | Done | Moves test-only constants, ScaledFont, image, raster-owner, and vector/backend C oracle helpers into `src/tests/oracle/native`, updates oracle packages to import that support package, and keeps oracle C symbols out of the public/native production binding packages |
| Glyph support package seam | Done | Moves the pure `Glyph` value type and glyph field-array preparation into `src/core/glyph`, re-exporting the type from the public facade with `pub type Glyph = @glyph.Glyph`; package-local tests cover pure invariants and marshaling order, and targeted API/context/oracle tests verify constructor, field, method, glyph-array FFI, and text-to-glyphs behavior still work through `CAIMEOX/cairoon` |
| Constants support package seam | Done | Moves generated primitive Cairo version/feature/MIME/tag/PDF/color-palette constants into `src/core/constants`, re-exports the same public names from the facade with `pub const` aliases, retargets `scripts/configure-link-flags.sh` to update/check the child package source of truth, and keeps public constants covered by package-local and direct-C oracle tests |
| Version implementation package seam | Done | Moves raw Cairo version extern declarations and UTF-8 decoding into `src/internal/version`, keeps public `@cairoon.cairo_version()` and `@cairoon.cairo_version_string()` as thin facade wrappers, and extends layout/link/verify scripts so internal native-gated packages are checked and tested like other support packages |
| Format implementation package seam | Done | Moves the raw `cairo_format_stride_for_width` extern into `src/internal/format`, keeps the public `Format` enum constructors and stride methods in `src/format.mbt`, and verifies typed/raw stride behavior through package-local, API, and surface tests |
| Status message implementation package seam | Done | Moves the raw `cairo_status_to_string` helper and UTF-8 decoding into `src/internal/status`, keeps the public `Status` enum constructors, `Status::message`, and `CairoError` suberrors in the facade, and verifies package-local plus API status/error mapping through the published package seam |
| PDF helper implementation package seam | Done | Moves raw PDF version count/version/string extern declarations and UTF-8 decoding into `src/internal/pdf`, keeps public `PDFVersion` constructors and `Surface::pdf*` wrappers in the facade, and verifies package-local plus surface black-box PDF version behavior through the published package seam |
| PS helper implementation package seam | Done | Moves raw PostScript level count/level/string extern declarations and UTF-8 decoding into `src/internal/ps`, keeps public `PSLevel` constructors and `Surface::ps*` wrappers in the facade, and verifies package-local plus surface black-box PS level behavior through the published package seam |
| SVG helper implementation package seam | Done | Moves raw SVG version count/version/string extern declarations and UTF-8 decoding into `src/internal/svg`, keeps public `SVGVersion` constructors and `Surface::svg*` wrappers in the facade, and verifies package-local plus surface black-box SVG version behavior through the published package seam |
| Stream callback helper implementation package seam | Done | Moves the stream callback chunk-copy helper into `src/internal/stream`, keeps public PDF/PS/SVG/PNG/script-device stream wrappers in the facade, removes the old root helper from the public package allowlist, and verifies package-local plus stream black-box behavior through the published package seam |
| C string helper implementation package seam | Done | Moves pure embedded-NUL byte scanning into `src/internal/cstring`, keeps public string/path validators and `CairoInvalidArgument(InvalidString, _)` error mapping in the facade, and verifies package-local plus existing font/context/surface invalid-string tests through the published package seam |
| Executable docs package split | Done | Moves the family executable reference docs from direct `src/*.mbt.md` files into `src/docs`, keeps `src/README.mbt.md` as the package readme, and verifies 50 docs examples through an external package that imports `CAIMEOX/cairoon` with Cairo link flags |
| Object trait facade colocation | Done | Moves external-object `Eq`/`Hash`/`Compare` impls from the grandfathered `src/traits.mbt` file into their owning facade wrapper files, deletes the old root file, and preserves the public trait surface through existing object black-box tests |
| FFI native target layout lint | Done | Extends `scripts/check-project-layout.py` so every production `src/**/ffi*.mbt` file outside `src/tests/` must be listed in the adjacent `moon.pkg` `targets` map as `[ "native" ]`, and stale native target entries for moved raw FFI files are rejected before MoonBit compilation |
| FFI ownership annotation lint | Done | Adds `scripts/check-ffi-ownership.py` and wires it into `scripts/verify.sh`, so every non-primitive raw C FFI parameter in production `src/**/ffi*.mbt` files, including internal helper packages, must be covered by `#borrow` or `#owned`; fixes the existing stream and raster-source callback trampoline annotations caught by the lint |
| Source size budget lint | Done | Splits the oversized ARGB32 image oracle test dispatcher into focused basic, pattern, pattern-stack, and pattern-operator scene files, adds `scripts/check-source-size-budget.py`, and wires it into `scripts/verify.sh` so source, test, script, native glue, and executable-doc files under `src/` and `scripts/` cannot exceed the 900-line review budget without an intentional split |
| Reliability ledger lint | Done | Adds `scripts/check-reliability-ledger.py` and wires it into `scripts/verify.sh`, so migration status rows use only the accepted statuses, `Partial` rows state their remaining gaps explicitly, the TESTING scorecard keeps all required reliability dimensions, and CI continues to run the native/ASan verify gates |
| Backend combo deep structure tags | Done | Broadens the existing backend feature/tag stream-combo scene with a third PDF outline and nested Document/Sect/H1/P/Span structure tags, keeping file-vs-stream equality, direct C oracle comparison, PDF marker checks, and PS/SVG inertness checks on the same scene |
| CI reliability workflow | Done | Adds `.github/workflows/ci.yml` with Ubuntu/macOS native `./scripts/verify.sh` jobs and a dedicated Ubuntu ASan verify job, using the official MoonBit CLI installer plus Cairo development packages |
| PDF rectangle tag markers | Done | Adds stable PDF marker checks for the three direct-C rectangle tag scenes, covering URI-link annotations, named destinations, and Document/Sect/H1/P structure-tree output |
| PDF text tag markers | Done | Tightens direct-C text-tag fixtures to in-page compact geometry, adds stable PDF marker checks for URI-link, named-destination, and Document/Sect/H1/P text tags, and extends PS/SVG negative tag-metadata checks to the URI-link text scene |
| PDF direct document-feature markers | Done | Adds stable PDF marker checks for the direct-C document-feature scene, covering metadata, custom metadata, page labels, outlines, URI links, named destinations, and Document/Sect/P structure output |
| Backend document-feature stream markers | Done | Adds stable marker checks for PDF/PS/SVG document-feature stream output, covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC/page markers, and SVG document-unit/page markers |
| PS/SVG backend-combo stream inertness | Done | Adds PS/SVG stream-output negative marker checks for the three-page backend feature/tag combo scene, proving stream callbacks also keep PDF annotation, URI, destination, and structure-tree metadata out of PostScript and SVG output |
| Callback invocation reference balance | Done | `cairoon_stream` and raster-source callback glue incref C-held MoonBit callback arguments before invoking MoonBit, and stream wrappers copy Cairo-provided chunks before user writers see them, so saved PDF/PS/SVG, PNG, and script stream data remains valid after callback return; adds a black-box retention test with allocation pressure |
| Backend stream callback multi-seed fuzz | Done | Adds a four-seed black-box stream callback fuzz over PDF/PS/SVG, PNG writer, script writer, PNG reader exact reads, PNG reader short reads, `WriteError`, invalid-status fallback, and reader-error paths |
| Finalizer graph multi-seed fuzz | Done | Adds a four-seed black-box finalizer/error-exit graph fuzz target covering surface/context/pattern/font/path/region/mapped-image/raster-source/script-device owner graphs, explicit and implicit mapped-image cleanup edges, expected Cairo errors, callback clearing, and allocation pressure under the targeted normal/ASan gate |
| Backend tag matrix differential output | Done | Adds scene 39 plus `surface_stream_tag_matrix_test.mbt`, covering a four-page PDF/PS/SVG file-vs-stream and direct-C-oracle tag matrix with nested document-structure tags, URI and destination links, `show_text_glyphs`, PDF metadata/custom-metadata overwrite/removal, page labels, outline parent/child mixtures, PDF/PS page-size changes, PS DSC markers, SVG document units, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| Backend lifecycle matrix differential output | Done | Adds scene 40 plus `surface_stream_lifecycle_test.mbt`, covering a five-page PDF/PS/SVG file-vs-stream and direct-C-oracle lifecycle matrix with consecutive retained copies, cleared and fresh pages, deeper document-structure tags including table/list labels, URI and destination links, `show_text_glyphs`, PDF metadata/custom-metadata overwrite/removal, page labels, outline parent/child/grandchild mixtures, PDF/PS page-size changes, PS Level 2 DSC markers, SVG millimeter units, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| Backend page-transition differential output | Done | Adds scene 41 plus `surface_stream_page_transition_test.mbt`, covering a three-page PDF/PS/SVG show-copy-show sequence with a cleared first page, retained copied page, retained overlay page, PDF metadata/custom-metadata deletion, page labels, outline parent/child links, URI and destination tags, Document/Sect/P/Figure/Span structure tags, PS DSC page-region markers, SVG point units, file-vs-stream equality, direct-C-oracle equality, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| Backend state-stack differential output | Done | Adds scene 42 plus `surface_stream_state_stack_test.mbt`, covering a three-page PDF/PS/SVG clip/group/mask state-stack sequence with clipped surface-pattern drawing, `push_group_with_content`/`pop_group_to_source` compositing, gradient mask painting, context-level `copy_page`, PDF metadata/custom-metadata deletion, page labels, outline parent/child links, URI and destination tags, Document/Sect/Figure/P/Span structure tags, PS DSC markers, SVG centimeter units, file-vs-stream equality, direct-C-oracle equality, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| Backend text-state differential output | Done | Adds scene 44 plus `surface_stream_text_state_test.mbt`, covering a three-page PDF/PS/SVG text-state sequence with font-matrix changes across retained and fresh pages, transformed `show_text` and `text_path` output under URI/destination/document-structure tags, PDF metadata/custom-metadata deletion, page labels, outline parent/child links, PS DSC markers, SVG pixel units, file-vs-stream equality, direct-C-oracle equality, stream-vs-direct-C equality through the backend oracle dispatcher, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| Context pycairo parity broadening | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived polygon `path_extents`, line `in_stroke`, and empty `stroke_extents` fixtures, raising that targeted parity file to five black-box tests under the normal and ASan gates |
| Context pycairo method fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived default user/device coordinate conversions, empty `select_font_face("")`, and `set_font_size(42)` font-matrix fixtures, raising that targeted parity file to eight black-box tests under the normal and ASan gates |
| Context pycairo state/source fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived current-point, dash-offset normalization, scalar line-width/miter-limit/tolerance setter, and source RGBA round-trip fixtures, raising that targeted parity file to twelve black-box tests under the normal and ASan gates |
| Context pycairo geometry/source fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived polygon `fill_extents`, empty `in_fill`, rectangle `path_extents`, relative path current-point, matrix setter/translate/scale/transform/rotate, and explicit source-pattern round-trip fixtures, raising that targeted parity file to sixteen black-box tests under the normal and ASan gates |
| Context pycairo font-object fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived font-face default/reset/custom round trips, `set_font_matrix` identity, font-options set/get, and scaled-font set/get fixtures, raising that targeted parity file to twenty black-box tests under the normal and ASan gates |
| Context pycairo clip/group/paint fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived copied `append_path` string, empty clip/reset `in_clip`, group push/pop/pop-to-source/content/unmatched-pop, and mask/mask-surface/alpha-paint fixtures, raising that targeted parity file to twenty-four black-box tests under the normal and ASan gates |
| Context pycairo drawing-state fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived default antialias/fill-rule/line-cap/line-join/operator getters, typed drawing-state enum setter round trips, raw operator C-int limit passthrough, and save/restore state restoration, raising that targeted parity file to twenty-eight black-box tests under the normal and ASan gates |
| Context pycairo text/glyph fixtures | Done | Extends `context_pycairo_parity_test.mbt` with pycairo-derived font/text extents, `show_text` current-point advance, `text_path` fillable path, `glyph_extents`, `show_glyphs` no-path rendering, `glyph_path` fillable path, and PDF no-output `show_text_glyphs` tuple-flow fixtures, raising that targeted parity file to thirty-one black-box tests under the normal and ASan gates |
| Context pycairo simple smoke fixture | Done | Extends `context_pycairo_parity_test.mbt` with pycairo's `test_simple` method-smoke flow covering `clip_preserve`, `copy_page`, `copy_path_flat`, `fill`, `fill_preserve`, `font_extents`, `identity_matrix`, `new_sub_path`, `show_page`, `stroke_preserve`, dash count, font matrix, group target, scalar getters, and final status, raising that targeted parity file to thirty-two black-box tests under the normal and ASan gates |
| Context pycairo hairline fixture | Done | Extends `context_pycairo_parity_test.mbt` with pycairo's `set_hairline`/`get_hairline` fixture and pairs it with a direct C ARGB32 hairline stroke oracle scene, raising that targeted parity file to thirty-three black-box tests under the normal and ASan gates |
| Context pycairo cmp/hash fixture | Done | Extends `context_pycairo_parity_test.mbt` with pycairo's `test_cmp_hash` behavior: a context equals itself, a new context on the same target is not equal, `Context::equal`/`Context::not_equal` agree with trait operators, and `Hash::hash` is callable and stable for the same wrapper, raising that targeted parity file to thirty-five black-box tests under the normal and ASan gates |
| Context pycairo parity test split | Done | Moves the font/text/glyph and simple-smoke pycairo fixtures into `context_font_text_pycairo_parity_test.mbt`, keeping the same 35 external black-box Context pycairo parity fixtures while reducing `context_pycairo_parity_test.mbt` from 675 to 471 lines and leaving public API unchanged |
| Context pycairo state/paint test split | Done | Moves the drawing-state, dash/scalar/source, copied-path, clip, group, and mask/alpha-paint pycairo fixtures into `context_state_paint_pycairo_parity_test.mbt`, preserving the same 35 external black-box Context pycairo parity fixtures while reducing `context_pycairo_parity_test.mbt` from 471 to 272 lines and leaving public API unchanged |
| Context pycairo tag fixture | Done | Extends `context_pycairo_parity_test.mbt` with pycairo's `test_tags` begin/end smoke fixture using image-surface no-op tag semantics; the context package now has 105 black-box tests under the normal gate |
| Context pycairo surface-source offsets | Done | Extends `context_state_paint_pycairo_parity_test.mbt` with pixel-level pycairo-derived coverage for `Context::set_source_surface` default and explicit offsets plus `Context::mask_surface` explicit offsets; the context pycairo package now has 38 external black-box fixtures under the normal gate |
| Pattern raw extend parity | Done | Adds `Pattern::set_extend_raw`/`Pattern::get_extend_raw`, covering pycairo's C-int extend passthrough for `42` while keeping typed `Pattern::get_extend` checked with `CairoInvalidArgument(InvalidStatus, _)` for unknown raw values |
| Pattern raw filter/dither parity | Done | Adds `Pattern::set_filter_raw`/`Pattern::get_filter_raw` and `Pattern::set_dither_raw`/`Pattern::get_dither_raw`, covering pycairo `pattern.c` C-int parsing for filter and dither while keeping typed getters checked with `CairoInvalidArgument(InvalidStatus, _)` for unknown raw values |
| Pattern raw enum documentation | Done | Extends `pattern.mbt.md` with an executable raw enum compatibility example covering `Pattern::set_extend_raw`, `Pattern::set_filter_raw`, and `Pattern::set_dither_raw`, documenting that unknown raw values remain visible through raw getters and are rejected by typed getters |
| Pattern pycairo raw extend fixture | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo's `test_set_extend` raw C-int passthrough behavior for `42`, proving the published package preserves the raw value through `get_extend_raw` while typed `get_extend` maps it to `CairoInvalidArgument(InvalidStatus, _)`; this raises the pattern pycairo package to eleven black-box tests under the normal gate |
| Pattern pycairo gradient/mesh fixtures | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo-derived linear-gradient point/color-stop list, radial-gradient circle, and mesh example patch fixtures, raising that targeted parity file to four black-box tests under the normal and ASan gates |
| Pattern pycairo cmp/hash fixture | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo's repeated-`Context.get_source()` comparison behavior: both wrappers compare equal, `not_equal` is false, `Pattern::equal` agrees with trait operators, and `Hash::hash` matches for the same Cairo pattern pointer, raising that targeted parity file to six black-box tests under the normal and ASan gates |
| Pattern pycairo mesh detail fixtures | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo's `test_mesh_pattern_example1` and `test_mesh_pattern_rest` behavior: Coons-patch construction, path extraction, corner-color queries, control-point queries, and invalid patch/corner/control-point index errors, raising that targeted parity file to eight black-box tests under the normal and ASan gates |
| Pattern pycairo independent identity fixture | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo's `test_pattern` identity/hash behavior for standalone solid patterns: the same wrapper compares equal and hashes consistently, while an independently allocated solid pattern with identical RGBA components compares unequal by Cairo pointer identity, raising that targeted parity file to nine black-box tests under the normal and ASan gates |
| Pattern pycairo mesh error-state fixture | Done | Extends `pattern_pycairo_parity_test.mbt` with pycairo's `test_mesh_pattern_error_states` lifecycle checks for nested `begin_patch`, out-of-patch `end_patch`, path commands, and corner-color setters, all mapping to `InvalidMeshConstruction`; the pattern family gates now have 46 black-box tests under the normal gate |
| Pattern pycairo raster-source fixtures | Done | Adds `pattern_raster_pycairo_parity_test.mbt` with pycairo `test_raster_source`-derived callback-state and acquire/release paint fixtures, keeping raster-source parity in its own small test file |
| Surface pycairo base fixtures | Done | Adds `surface_pycairo_parity_test.mbt` with pycairo `test_surface.py`-derived context-manager, cmp/hash, content/format getter, device-scale, device-offset, fallback-resolution, dirty/page operation, `create_for_rectangle`, `create_similar`, and `create_similar_image` fixtures, including returned image usability after the creating PDF surface scope exits |
| Surface/context test organization split | Done | Moves buffer-backed image-surface tests into `image_surface_data_test.mbt`, PNG path round-trip tests into `surface_png_test.mbt`, and the context invalid-restore state test into `context_state_test.mbt`, keeping the same external black-box behavior while reducing `surface_context_test.mbt` from 525 to 348 lines and leaving public API unchanged |
| Surface image-property test organization split | Done | Moves image-surface property, raw-format/raw-content, finished/subtype error, and Cairo float-format black-box tests into `surface_image_properties_test.mbt`, preserving the same external behavior while reducing `surface_context_test.mbt` from 348 to 246 lines and leaving public API unchanged |
| Vector backend output test organization split | Done | Splits the mixed PDF/PS/SVG marker tests from `vector_output_test.mbt` into `vector_output_pdf_test.mbt`, `vector_output_ps_test.mbt`, and `vector_output_svg_test.mbt`, preserving the same oracle and marker assertions while reducing the largest vector-output marker file from 490 to 383 lines and leaving public API unchanged |
| Vector output oracle test organization split | Done | Splits the direct-C vector oracle tests from `vector_output_oracle_test.mbt` into focused basic, tag/text-tag, page/glyph/multipage, and PDF document-feature files, preserving the same oracle, marker, and inert-tag assertions while reducing `vector_output_oracle_test.mbt` from 441 to 25 lines and leaving public API unchanged |
| Lifetime stream stress test organization split | Done | Splits stream callback lifetime stress coverage from `lifetime_stream_stress_test.mbt` into focused helper, allocation-stress, and multi-seed fuzz files, preserving the same 18 lifetime black-box tests while reducing the largest stream lifetime test file from 437 to 222 lines and leaving public API unchanged |
| Surface stream test organization split | Done | Splits public stream callback tests from `surface_stream_test.mbt` into shared helpers plus focused vector-stream, PNG-stream, and retained-chunk files, preserving the same 13 stream black-box tests while reducing the largest stream callback file from 383 to 163 lines and leaving public API unchanged |
| Context raw drawing-state enum parity | Done | Adds `Context::set_antialias_raw`/`Context::get_antialias_raw`, `Context::set_fill_rule_raw`/`Context::get_fill_rule_raw`, `Context::set_line_cap_raw`/`Context::get_line_cap_raw`, and `Context::set_line_join_raw`/`Context::get_line_join_raw`, covering pycairo `context.c` C-int parsing while keeping typed getters checked with `CairoInvalidArgument(InvalidStatus, _)` for unknown raw values |
| FontOptions raw enum parity | Done | Adds `FontOptions::set_antialias_raw`/`FontOptions::get_antialias_raw`, `FontOptions::set_subpixel_order_raw`/`FontOptions::get_subpixel_order_raw`, `FontOptions::set_hint_style_raw`/`FontOptions::get_hint_style_raw`, `FontOptions::set_hint_metrics_raw`/`FontOptions::get_hint_metrics_raw`, and `FontOptions::set_color_mode_raw`/`FontOptions::get_color_mode_raw`, covering pycairo `font.c` C-int parsing while keeping typed getters checked with `CairoInvalidArgument(InvalidStatus, _)` for unknown raw values |
| Toy font raw slant/weight parity | Done | Adds `FontFace::toy_raw`, `FontFace::get_slant_raw`, `FontFace::get_weight_raw`, and `Context::select_font_face_raw`, covering pycairo `font.c`/`context.c` C-int parsing for toy font slant/weight. Known raw values round trip through raw getters and invalid raw values map to Cairo's `InvalidSlant`/`InvalidWeight` status, matching pycairo's post-construction status checks |
| PS/SVG tagged multi-page inertness | Done | Adds negative PDF tag-metadata marker checks for the two-page tagged multi-page text scene on PostScript and SVG, covering URI links, named destinations, Document/Sect/P tags, and backend inertness after direct C oracle parity |
| Backend feature/tag stream combo | Done | Adds PDF/PS/SVG file-vs-stream normalized equality, direct C oracle comparison, stable PDF/PS/SVG marker checks, and PS/SVG negative file/stream tag-metadata marker checks for one three-page scene that combines backend-specific document features (PDF metadata/page labels/outlines, PS DSC, SVG version/unit), URI/destination/document-structure tags, surface patterns, text, and `show_text_glyphs` |
| Raster-source manual callback fuzz | Done | Adds a three-seed white-box state-machine test for `raster_get_callbacks()` manual acquire/release calls across release-only, acquire-only, acquire+release, failed-source, and dynamic compatible-source paths, with pixel readback and owner-count balance after every transition |
| Standalone text stream equivalence | Done | Adds PDF/PS/SVG file-vs-stream normalized equality for the standalone toy-font `text_path` and `show_text` vector scenes that previously had direct C output oracles but no stream constructor equality check |
| PDF page-operation document-feature output | Done | Scene 34 adds a PDF-only direct C oracle, marker test, and file-vs-stream normalized equality for `Context::copy_page` retaining a two-page document that also exercises PDF metadata, custom metadata, page labels, outlines, URI links, named destinations, document-structure tags, and retained text drawing |

## Top-Level Functions And Module Constants

| pycairo API | cairoon status | Notes |
|---|---|---|
| `cairo_version()` | Done | Exposed as `cairo_version()` |
| `cairo_version_string()` | Done | Exposed as `cairo_version_string()` |
| `CAIRO_VERSION*` constants | Done | Compile-time Cairo version constants exposed and white-box tested against C macros |
| `HAS_*` feature constants | Done | Feature flags exposed as Boolean constants and white-box tested against C macros for this native build |
| `MIME_TYPE_*` constants | Done | Cairo MIME strings exposed and white-box tested against C macros |
| `TAG_*` constants | Done | Cairo tag strings exposed and white-box tested against C macros |
| `PDF_OUTLINE_ROOT`, `FORMAT_INVALID`, `COLOR_PALETTE_DEFAULT` | Done | Numeric constants exposed and white-box tested against C macros |
| `version`, `version_info` | Decision | Resolved out of public API scope: Cairo library version is exposed through `cairo_version*` and package version remains Moon package metadata in `moon.mod` |
| `get_include()` | Decision | Resolved out of public API scope: pycairo uses this for Python C-extension header discovery; cairoon uses `moon.pkg` native link configuration instead |
| `CAPI` | Decision | Resolved out of public API scope: pycairo exposes this CPython extension capsule table for third-party Python extension modules; cairoon exposes native MoonBit wrappers and does not ship a Python C-extension ABI |
| Legacy enum alias constants such as `ANTIALIAS_*`, `STATUS_*`, `OPERATOR_*` | Decision | MoonBit canonical API uses typed enum constructors such as `AntialiasBest`, `Success`, and `OperatorOver`; uppercase MoonBit names are restricted to primitive `const` values, so enum-typed pycairo aliases cannot be represented directly, and exposing weak `Int` aliases would undermine the typed API |

## Enums And Constants

| pycairo enum | cairoon status | Notes |
|---|---|---|
| `Antialias` | Done | Constructors use `Antialias*` prefixes |
| `Content` | Done | Cairo bit values preserved; pycairo-compatible raw C-int content arguments are available on surface, context-group, recording/script surface, and raster-source constructors, with unknown raw content mapped to `CairoInvalidArgument(InvalidContent, _)` before entering Cairo |
| `SurfaceType` | Done | Values through Cairo 1.18.4 are listed for `Surface::get_type` |
| `DeviceType` | Done | Valid non-negative values through Cairo 1.18.4 are listed for `Device::get_type`; `INVALID = -1` is not represented by the enum |
| `FillRule` | Done | Constructors use `Fill*` prefixes |
| `Format` | Done | Valid image formats are represented as non-negative enum constructors; Cairo's `CAIRO_FORMAT_INVALID = -1` is exposed separately as the integer sentinel `FORMAT_INVALID` because MoonBit enum discriminants are non-negative. Typed and pycairo-compatible raw `stride_for_width` helpers are exposed, raw format constructors/getters cover image, image-for-data, similar-image, and mapped-image surfaces, typed getters reject unknown raw values, and tests cover legacy formats, 16-bit/30-bit formats, Cairo float formats, invalid raw format, and invalid negative widths |
| `HintMetrics` | Done | Constructors use `HintMetrics*` prefixes |
| `ColorMode` | Done | Cairo 1.18 font color options |
| `Dither` | Done | Cairo 1.18 pattern dither enum and pattern methods |
| `HintStyle` | Done | Constructors use `HintStyle*` prefixes |
| `SubpixelOrder` | Done | Constructors use `Subpixel*` prefixes |
| `LineCap` | Done | Constructors use `LineCap*` prefixes |
| `LineJoin` | Done | Constructors use `LineJoin*` prefixes |
| `Filter` | Done | Pattern filter implemented; public constructor coverage includes the pycairo enum smoke set |
| `Operator` | Done | Constructors use `Operator*` prefixes |
| `Extend` | Done | Pattern extend implemented; public constructor coverage includes the pycairo enum smoke set, and `ExtendNone` is used to avoid colliding with `Option::None` |
| `FontSlant` | Done | Needed by toy font APIs |
| `FontWeight` | Done | Needed by toy font APIs |
| `Status` | Done | Values through Cairo 1.18.4 are listed; exhaustive `CairoError` classification is tested, with pycairo `Status.JBIG2_GLOBAL_MISSING` smoke coverage mapped to `Jbig2GlobalMissing` |
| `PDFVersion` | Done | Needed by `PDFSurface` |
| `PSLevel` | Done | Needed by `PSSurface` |
| `PathDataType` | Done | Needed by `Path` iteration |
| `RegionOverlap` | Done | Needed by `Region` |
| `SVGVersion` | Done | Needed by `SVGSurface` |
| `SVGUnit` | Done | Needed by `SVGSurface` |
| `PDFMetadata` | Done | Needed by `PDFSurface` metadata |
| `PDFOutlineFlags`, `PDFOutlineFlagSet` | Done | Cairo bit values preserved; `PDFOutlineFlagSet` provides typed combinations for outline APIs |
| `ScriptMode` | Done | Needed by script device |
| `TextClusterFlags` | Done | Cairo bit value preserved; pycairo `TextClusterFlags.BACKWARD` smoke coverage maps to `TextClusterBackward` |
| `SurfaceObserverMode` | Done | pycairo exposes only the enum; pycairo `SurfaceObserverMode.NORMAL` smoke coverage maps to `SurfaceObserverNormal`; Cairo native observer surface/device APIs are outside the pycairo-migration API surface unless cairoon later grows an explicit Cairo-extension layer |
| Tag and MIME string constants | Done | `TAG_*` and `MIME_TYPE_*` expose Cairo strings and are checked against compile-time C macros |
| Legacy uppercase enum alias constants | Decision | Not exposed as values. MoonBit cannot define uppercase enum-typed aliases because non-primitive constants are not allowed, and lowercase aliases would not match pycairo's legacy API names. Use the typed enum constructors listed above. |

## Value Types

| pycairo API | cairoon status | Notes |
|---|---|---|
| `Matrix` | Done | MoonBit pure-value API returns transformed copies; `component`/`components` cover pycairo indexing/unpacking semantics including invalid positive/negative indexes, pycairo-derived fixtures cover equality, `init_rotate(0)`, invert, multiply, translate, scale back to identity, rotate, and identity point/distance transforms, deterministic property tests cover multiplication associativity, inverse identity, composed point/distance transforms, and distance-vs-point-delta behavior, and executable reference docs cover pure-value transform usage |
| `Path` | Done | Opaque owned `cairo_path_t *`, status/equality/hash, MoonBit `Eq`/`Hash`/`Compare` traits, pycairo-derived fixtures for empty/path stringification, self-comparison/hash, and iterator data, pycairo-compatible stringification including close-path continuation formatting, segment values, copied-path lifetime after the source context exits, targeted normal/ASan gate coverage, and executable reference docs |
| `Rectangle` | Done | Pure value type with field/component access, MoonBit `Eq`/`Hash`, pycairo tuple-like parity fixtures, numeric-limit coverage, `Context::copy_clip_rectangle_list` return coverage, and `RecordingSurface` extents coverage |
| `RectangleInt` | Done | Pure value type with defaults, MoonBit `Eq`/`Hash`, and pycairo-derived 32-bit boundary field/component coverage; needed by region, map-to-image, and raster-source acquire extents |
| `Glyph` | Done | Pure value type implemented in `src/core/glyph` and re-exported by the public facade, with MoonBit `Eq`/`Hash`; `UInt64` index matches Cairo's `unsigned long` width on 64-bit platforms, and pycairo tuple-like value plus limit fixtures cover fields/components while array marshaling remains separate |
| `TextCluster` | Done | Pure value type with MoonBit `Eq`/`Hash`, pycairo tuple-like field/component/hash fixtures, and 32-bit integer limit coverage; array marshaling remains separate |
| `TextExtents` | Done | Pure value type with MoonBit `Eq`/`Hash`, pycairo tuple-like field/component/hash fixtures, numeric-limit coverage, and Context/ScaledFont text/glyph extents return coverage |
| Font extents tuple | Done | Exposed as named `FontExtents` pure value per `AGENTS.md`, with MoonBit `Eq`/`Hash`, field/component/hash fixtures, and Context/ScaledFont font-extents return coverage |

## Pattern APIs

| pycairo API | cairoon status | Notes |
|---|---|---|
| `Pattern.get_extend/set_extend` | Done | Implemented on opaque `Pattern` |
| `Pattern.get_filter/set_filter` | Done | Implemented on opaque `Pattern` |
| `Pattern.get_matrix/set_matrix` | Done | Primitive-field stub avoids struct layout reliance |
| `Pattern.get_dither/set_dither` | Done | Cairo 1.18 feature implemented on opaque `Pattern` |
| `SolidPattern.__init__/get_rgba` | Done | Exposed as `Pattern::solid_rgb/solid_rgba/get_rgba`; tests cover alpha defaults and RGBA round-trip |
| `SurfacePattern.__init__` | Done | Exposed as `Pattern::for_surface`; retains base `Surface` wrapper and is covered by paint/readback and lifetime tests |
| `SurfacePattern.get_surface` | Done | Returns a referenced `Surface` wrapper and maps non-surface patterns to `PatternTypeMismatch` |
| `Gradient.add_color_stop_*` | Done | Implemented on opaque `Pattern` |
| `Gradient.get_color_stops_rgba` | Done | Returns a copied `Array[(Double, Double, Double, Double, Double)]`; tests cover pycairo-style RGB/RGBA stop tuples, duplicate-offset insertion order, and snapshot stability after later pattern mutation |
| `LinearGradient` | Done | Exposed as `Pattern::linear` plus `get_linear_points` |
| `RadialGradient` | Done | Exposed as `Pattern::radial` plus `get_radial_circles` |
| `MeshPattern` | Done | Exposed as `Pattern::mesh` and `Pattern::mesh_*` methods for patch lifecycle, move/line/curve commands, control points, corner colors, patch count, path extraction, and query helpers; tests cover pycairo examples including triangle patch construction, the curve-first patch path case, `test_mesh_pattern_error_states` lifecycle errors, invalid indexes, non-mesh mismatch, painting smoke, and targeted ASan gate coverage |
| `RasterSourcePattern` | Partial | Exposed as `Pattern::raster_source` and `Pattern::raster_source_raw`, compatible `raster_set_acquire`/`raster_get_acquire`/`raster_clear_acquire`, and pycairo-style optional `raster_set_callbacks`/`raster_get_callbacks`/`raster_clear_callbacks` for acquire-only, acquire+release, release-only, and clear states; C callback trampolines retain MoonBit closures and acquired surface wrappers, and acquire-only states still install cairoon's internal release trampoline so retained wrappers are released even without a user release closure. Tests cover raw C-int content construction plus `InvalidContent` for unknown raw content, pycairo-derived callback-state and acquire/release paint fixtures, paint output, release callbacks, release-only state, get-acquire callback identity/optional/clear behavior, compatible target/extents acquire rendering with device-offset source surfaces, data-backed source lifetime, retained-owner stress, white-box owner-count assertions for repeated acquire-only same-surface paints, a 64-step acquire-only replacement fuzz, stale release-to-acquire-only replacement, failed acquire replacement owner-count fuzz, a four-seed 72-step-per-seed callback state-machine fuzz covering clear, release-only, acquire-only, acquire+release, failed acquire, dynamic compatible-source, callback-introspection, and owner-count balance transitions, and a three-seed manual get-callback acquire/release fuzz covering release-only, acquire-only, acquire+release, failed-source, dynamic compatible-source, pixel readback, and owner-count balance transitions, subtype mismatch, finished-surface acquire failure injection, post-failure acquire replacement recovery, deterministic 25-step callback replacement/failure fuzz with dynamic compatible source surfaces, direct C ARGB32 oracle repeat rendering, and 1000-iteration callback allocation stress covering set/get/manual acquire/release/replace/clear paths. Broader randomized/platform fuzz remains. |

## Surface And Device APIs

| pycairo API | cairoon status | Notes |
|---|---|---|
| `Device` | Done | Exposed as opaque `Device` with script constructor, status/type/equal/hash, MoonBit `Eq`/`Hash` traits, finish/flush/acquire/release, `with_acquired`, and `with_finished`; pycairo-derived tests cover device reference/equality/hash behavior, image-surface `get_device is None`, script-device context-manager cleanup on success and Cairo errors, and `DeviceFinished` after finish |
| `Surface` base methods | Done | Finish/with_finished/flush/copy_page/show_page/write_to_png(path/stream), create_similar/create_similar_raw/create_similar_image/create_similar_image_raw/create_for_rectangle, typed/raw content and format/type, pointer equality/hash, MoonBit `Eq`/`Hash` traits, get_device, dirty markers, device offset/scale, fallback resolution, show-text-glyphs support query, MIME data, and get_font_options are implemented. pycairo-derived fixtures cover surface context-manager cleanup, target equality/hash through `Context::get_target`, content/format getters, device-scale valid/extreme/invalid-matrix cases, device-offset normal/extreme round trips, fallback-resolution round trips, dirty/page operations, `create_for_rectangle` success/invalid-size/extreme numeric cases, `create_similar` success/invalid-size cases, and PDF-backed `create_similar_image` including returned-image usability after the creating PDF surface scope exits. `finish()` is idempotent, `with_finished` mirrors pycairo's surface context-manager cleanup on success and Cairo errors, and `status`, `flush`, `copy_page`, `show_page`, `has_show_text_glyphs`, dirty markers, device offset/scale, and fallback-resolution wrappers after finish are covered for `SurfaceFinished`. `create_for_rectangle` retains its parent wrapper and is covered through parent-surface clipping/translation rather than backend-dependent surface type, including pycairo numeric-limit success paths. `create_similar`, `create_similar_raw`, `create_similar_image`, `create_similar_image_raw`, and `create_for_rectangle` map invalid dimensions, content, or format to checked `CairoInvalidArgument` statuses. Device offset/scale cover pycairo extreme double round-trips. Surface::copy_page/Surface::show_page retained-page behavior has PDF/PS/SVG direct C oracle coverage, a PDF two-page marker check, and stream-vs-file normalized equality coverage. Surface::show_page cleared-page behavior has PDF/PS/SVG direct C oracle coverage, a PDF two-page marker check, and stream-vs-file normalized equality coverage. Surface base, image-family, subsurface, MIME, backend-surface, device/stream, and object-trait tests are included in the targeted normal/ASan verification gate. Backend-specific metadata/output helpers remain tracked in the `PDFSurface`, `PSSurface`, `SVGSurface`, and MIME rows rather than the base Surface row. |
| `ImageSurface` | Done | Basic typed/raw creation, buffer-backed typed/raw creation, PNG path/stream loading, copied readback, retained mutable `ImageData` view, `get_data` after finish for ordinary/buffer-backed/PNG-loaded surfaces, width/height/stride/typed-format/raw-format and copied-data getters with `SurfaceFinished` and non-image `SurfaceTypeMismatch` mapping, retained view invalidation after base `finish()`, `Rgb96F`/`Rgba128F` float-format construction/readback, compact pycairo-derived `create_for_data` array readback fixture, retained-buffer release-on-finish fixture, raw format invalid-status coverage, direct C ARGB32 oracle rendering coverage, and executable Surface reference docs |
| `ImageSurface.create_for_data` | Done | Exposed as `Surface::image_for_data` and pycairo-compatible `Surface::image_for_data_raw` for `FixedArray[Byte]`; retains the MoonBit buffer, validates size/stride, covers default-stride prefilled ARGB32 array wrapping and byte-for-byte copied readback, covers zero-size buffers, shares storage with `ImageData`, maps invalid raw default-stride format to `CairoInvalidArgument(InvalidFormat, _)`, maps `get_data` after finish to `SurfaceFinished`, releases retained MoonBit buffers during `Surface::finish`, and has forty-four-scene direct C Cairo ARGB32 oracle coverage for the backing-buffer path including source-surface offset, mask-surface offset, raster-source pattern, dashed round-cap stroke, clipped paint/fill, `OperatorClear` compositing semantics, group compositing semantics, mask pattern compositing semantics, even-odd fill-rule semantics, surface-pattern `Reflect`/`Nearest`/`DitherBest`/matrix combination semantics, transformed repeated linear-gradient `Repeat`/`Nearest`/`DitherFast`/matrix combination semantics, transformed radial-gradient `Reflect`/`Nearest`/`DitherGood`/matrix combination semantics, surface-pattern `Pad`/`Bilinear`/`DitherDefault`/matrix semantics, surface-pattern `ExtendNone`/`Nearest`/`DitherNone`/matrix semantics, transformed linear-gradient `Pad`/`Bilinear`/`DitherBest`/matrix semantics, transformed radial-gradient `Repeat`/`Bilinear`/`DitherFast`/matrix semantics, surface-pattern `Repeat`/`Bilinear`/`DitherGood`/matrix semantics, transformed linear-gradient `ExtendNone`/`Good`/`DitherNone`/matrix semantics with duplicate-offset stops, transformed radial-gradient `Pad`/`Good`/`DitherDefault`/matrix semantics, curve/multi-patch mesh-pattern matrix semantics, nested surface/linear/radial/mask pattern-stack clip/save/restore/OperatorAtop semantics, mesh/linear surface-mask group-compositing semantics, set_source_surface/repeated-surface-pattern/radial-mask group-compositing semantics, surface-pattern device-offset mask with `OperatorScreen` group-compositing semantics, surface/radial-gradient `OperatorOverlay` group-mask semantics, mesh/surface/linear `OperatorSoftLight` group-mask semantics, surface-pattern stroke `OperatorDifference` group-mask semantics, radial-gradient `OperatorColorDodge` over transformed reflected surface-pattern semantics, transformed repeated surface-pattern plus reflected linear-gradient `OperatorHslHue` group-mask semantics, and hairline stroke semantics |
| `ImageSurface.create_from_png/write_to_png` | Done | Filename APIs exposed as `Surface::image_from_png` and `Surface::write_to_png`; PNG stream APIs are exposed as `Surface::image_from_png_stream` and `Surface::write_to_png_stream`. Writers pass copied bytes to user callbacks so saved chunks remain valid after the callback returns, and map `WriteError` plus invalid-status fallback to `WriteError`; readers require exact-length chunks and map short/null reads to `ReadError`/`PngError`. PNG stream write/read, stream `WriteError`, invalid-status fallback, exact-read, and short-read paths have 1000-iteration callback allocation stress plus multi-seed callback fuzz, PNG-loaded image surfaces map `get_data` after finish to `SurfaceFinished`, and Python file-object callbacks are represented by explicit MoonBit stream callbacks per `AGENTS.md` |
| `Surface.map_to_image/unmap_image` | Done | `MappedImageSurface` has a dedicated payload retaining the base `Surface`, explicit and finalizer unmap support, typed/raw format query, `MappedImageSurface::with_unmapped` for pycairo-style mapped-image context-manager cleanup on success and Cairo errors, including upload-before-error propagation coverage, reuse-after-scope `SurfaceFinished` checks for repeated unmap/data access/scoped cleanup, extent mapping, Context construction, mutable `ImageData` view with post-unmap `SurfaceFinished` checks, double/wrong-base checks, retained-owner stress, targeted ASan coverage, and executable Surface reference docs |
| `Surface.get_mime_data/set_mime_data/supports_mime_type` | Done | Exposed on `Surface`; data is copied into C-owned storage on set and copied back into MoonBit `Bytes` on get. This intentionally does not preserve pycairo's Python object identity for data set through the binding. Tests cover storage/clear, pycairo-derived same-payload multi-key storage with per-key clearing, embedded NUL bytes, zero-length payloads, invalid MIME type strings, image/PDF/PS/SVG backend-specific MIME support matrices, and PDF JPEG MIME payload passthrough with PDF direct C oracle coverage; broader backend-specific MIME emission coverage remains tracked in the backend surface and Tests rows |
| `PDFSurface` | Partial | Exposed as `Surface::pdf`, `Surface::pdf_stream`, `PDFVersion::supported`, `PDFVersion::to_string`, `Surface::pdf_restrict_to_version`, size, metadata, custom metadata, page label, thumbnail, single-flag outline compatibility, and typed outline flag sets. Filename/no-output/stream constructors, executable backend reference docs, subtype-mismatch checks for every bound PDF helper, PDF structural markers, metadata/custom-metadata/page-label/outline output markers, page thumbnail output markers plus direct C oracle comparison, multi-page output markers, tagged multi-page text structure markers, rectangle/text tag marker checks, direct document-feature marker checks, tagged `show_text_glyphs` Link/structure marker checks, grouped glyph/tag combined marker checks, mixed vector/tag/text combined tag markers, layered three-page combined tag markers, wide three-page combined tag markers, combined two-page metadata/custom-metadata/page-label/outline/URI/named-destination/document-structure marker coverage, thirty-four-scene direct C oracle comparison for deterministic PDF 1.4 vector/tag/document-feature/page-operation/backend-combo output including toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern, mask surface, mesh pattern, URI-link tag, named-destination tag, document-structure tag scenes, URI-link text tag, named-destination text tag, document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, one drawing-oriented two-page metadata/custom-metadata/page-label/outline/tag document-feature scene, one text/tag-aware combined metadata/custom-metadata/page-label/outline/URI/named-destination/document-structure scene, one page-operation metadata/custom-metadata/page-label/outline/URI/named-destination/document-structure scene, and backend feature/tag stream-combo through research-note scenes, backend-combo PDF marker coverage, PDF JPEG MIME payload embedding and PDF thumbnail output with direct C oracle comparison, stream `WriteError` and invalid-status fallback mapping, stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, PDF text document-feature, PDF page-operation document-feature, PDF JPEG MIME and PDF thumbnail output, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend feature/tag stream-combo output, deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks, and 1000-iteration stream callback allocation stress are covered; broader normalized PDF tag/metadata/multi-page output tests remain. |
| `PSSurface` | Partial | Exposed as `Surface::ps`, `Surface::ps_stream`, `PSLevel::supported`, `PSLevel::to_string`, `Surface::ps_restrict_to_level`, EPS get/set, size, and DSC helpers. Filename/no-output/stream constructors plus executable backend reference docs, pycairo DSC setup-order fixture, subtype-mismatch checks for every bound PS helper, PS page/drawing and multi-page markers, combined language-level/DSC setup/page-setup/multi-page output markers, seventeen-scene direct C oracle comparison including toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern, mask surface, and mesh pattern scenes with `CreationDate` normalization, normalized direct C oracle comparison for the combined DSC/multi-page feature scene, Link tag inertness, destination/document-structure tag scenes, URI-link/named-destination/document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, and one backend-feature/tag stream-combo oracle scene, negative marker checks that destination/document-structure, tagged multi-page text, tagged `show_text_glyphs`, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, and backend-combo file/stream tag scenes do not emit PDF tag metadata, backend-combo PS marker coverage, stream `WriteError` and invalid-status fallback mapping, stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend feature/tag stream-combo output, deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks, and 1000-iteration stream callback allocation stress are covered; broader normalized PS tag output tests remain. |
| `SVGSurface` | Partial | Exposed as `Surface::svg`, `Surface::svg_stream`, `SVGVersion::supported`, `SVGVersion::to_string`, `Surface::svg_restrict_to_version`, and document-unit get/set. Filename/no-output/stream constructors plus executable backend reference docs, subtype-mismatch checks for every bound SVG helper, finished-status coverage for document-unit getters/setters and version restriction, SVG geometry/color markers, multi-page output markers, combined version-restricted/document-unit/multi-page output markers, normalized seventeen-scene direct C Cairo oracle comparison including toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, two-page paint, clip, dashed stroke, repeated surface pattern with dynamic `source-*` id normalization, mask surface, and mesh pattern scenes, normalized direct C oracle comparison for the combined version/unit/multi-page feature scene, Link tag inertness, destination/document-structure tag scenes, URI-link/named-destination/document-structure text tag scenes, tagged multi-page text, one copy_page retained two-page drawing scene, one Surface::copy_page/Surface::show_page retained two-page primitive oracle, one Surface::show_page cleared two-page primitive oracle, one tagged `show_text_glyphs` scene, one grouped multi-page glyph/tag scene, one mixed vector/tag/text scene, one layered three-page clip/dash/surface-pattern/mask/tag/text scene, one wide three-page URI/destination/document-structure tag/vector scene, and one backend-feature/tag stream-combo oracle scene, negative marker checks that destination/document-structure, tagged multi-page text, tagged `show_text_glyphs`, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, and backend-combo file/stream tag scenes do not emit PDF tag metadata, backend-combo SVG marker coverage, stream output, WriteError, and invalid-status fallback mapping, stream-vs-file normalized output equality for non-text primitive paint/stroke/fill/path/transform/gradient/multi-page/clip/dash/surface-pattern/mask/mesh scenes, deterministic two-page, copy-page retained two-page, Surface::copy_page retained two-page, Surface::show_page cleared two-page, tagged three-page, single-page URI-link/named-destination/document-structure rectangle/text tag scenes, tagged `show_text_glyphs`, grouped glyph/tag multi-page, standalone toy-font text_path/show_text vector scenes, glyph_path/show_glyphs vector scenes, mixed vector/tag/text, layered three-page clip/dash/surface-pattern/mask/tag/text, wide three-page tag/vector, backend document-feature scenes covering PDF metadata/custom metadata/page labels/outlines/tags, PS DSC, and SVG document units, backend feature/tag stream-combo output, deep tag tree stream-to-direct-C normalized oracle comparison, plus PS/SVG stream negative tag-metadata checks, and 1000-iteration stream callback allocation stress are covered; broader normalized SVG tag combinations remain. |
| `RecordingSurface` | Done | Exposed as `Surface::recording`, `Surface::recording_raw`, `Surface::recording_get_extents`, and `Surface::recording_ink_extents`; tests and executable backend reference docs cover bounded/unbounded extents, pycairo post-finish `recording_get_extents` behavior, pycairo raw C-int content construction plus `InvalidContent` for unknown raw content, replay through `Pattern::for_surface`, and `SurfaceTypeMismatch` for non-recording surfaces |
| `ScriptDevice` / `ScriptSurface` | Done | File-path and stream `Device::script` constructors exist as `Device::script` and `Device::script_stream`; mode, comments, recording replay, `Surface::script`, `Surface::script_raw`, and `Surface::script_for_target` exist. Executable backend reference docs, pycairo raw C-int content construction plus `InvalidContent` for unknown raw content, pycairo-derived `HAS_SCRIPT_SURFACE`, script device type, file-path constructor with script-content readback, acquire/release, default/binary mode, write-comment, and recording replay output fixtures, stream output/error callbacks including invalid-status fallback to `WriteError`, device/surface retained-owner behavior, scoped `Device::with_finished`, subtype/string error checks, 1000-iteration stream callback allocation stress, and multi-seed script stream callback fuzz are covered. Python file-like object behavior is explicitly out of scope per `AGENTS.md` and represented by explicit MoonBit stream callbacks. |
| `TeeSurface` | Done | Exposed as `Surface::tee`, `Surface::tee_add`, `Surface::tee_remove`, and `Surface::tee_index`; tests and executable backend reference docs cover mirrored drawing, index wrappers, subtype errors, negative-index `InvalidIndex` handling, positive out-of-range Cairo `NoMemory` status handling matching pycairo's `MemoryError` path, retained primary/target wrappers, and targeted ASan coverage on this `HAS_TEE_SURFACE` build |
| `PDFSurface.set_thumbnail_size` pycairo parity | Done | `surface_pdf_test.mbt` mirrors pycairo's thumbnail resize/reset sequence by accepting `10x10`, `0x0`, and `1x1` sizes before finish, then mapping a post-finish thumbnail update to checked `SurfaceFinished`. Output-level thumbnail emission remains covered by the PDF direct C oracle and stream-to-oracle rows. |
| `Win32Surface` / `Win32PrintingSurface` | Decision | Out of the first portable full-product scope per `AGENTS.md`; requires a future conditional extension layer with Win32 headers, link flags, and CI/test environment |
| `XCBSurface` / `XlibSurface` | Decision | Out of the first portable full-product scope per `AGENTS.md`; requires a future conditional extension layer with X11/XCB headers, link flags, and CI/test environment |

## Backend Differential Slices

| Slice | Status | Notes |
|---|---|---|
| `PDFSurface` page-operation differential output | Done | Scene 34 covers a `Context::copy_page` retained-page PDF with metadata, custom metadata, page labels, outlines, URI-link and named-destination tags, document-structure tags, retained text drawing, direct C output equivalence, stable marker assertions, and stream-vs-file normalized equality |
| PDF/PS/SVG backend stream-combo matrix | Done | Scene 35 covers a three-page cross-backend output with PDF metadata/page labels/outlines/custom metadata, PS DSC setup/page markers, SVG document units, URI-link and named-destination tags, `Document`/`Sect`/`H1`/`P`/`Span` structure tags, surface-pattern painting, toy text, `show_text_glyphs`, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend surface-page feature/tag combo | Done | Scene 36 covers a three-page cross-backend output that combines `Surface::copy_page` retained-page output, `Surface::show_page` cleared output, PDF metadata/custom metadata/page labels/outlines, PS DSC setup/page markers, SVG document units, URI-link and named-destination tags, document-structure tags, `show_text_glyphs`, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG nested tag/page sequence | Done | Scene 37 covers a four-page cross-backend output with deeper PDF document-structure nesting (`Document`/`Sect`/`H1`/`Figure`/`L`/`LI`/`LBody`/`P`/`Span`), URI and destination links, retained-page `Surface::copy_page` output, `Surface::show_page`, nested PDF outline parents/children, richer metadata/custom metadata, page labels, PS DSC markers, SVG document units, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG resized page-sequence feature combo | Done | Scene 38 covers a four-page cross-backend output with alternating `Surface::copy_page`/`Surface::show_page`, PDF/PS page-size changes, PDF metadata/custom metadata overwrite/removal, page labels, nested outline parent/child mixtures, URI and named-destination links, document-structure tags, PS DSC markers, SVG document units, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend tag matrix | Done | Scene 39 combines the scene 37 deep-tag and scene 38 page-sequence dimensions: four pages, retained and cleared page operations, nested `Document`/`Sect`/`H1`/`Figure`/`L`/`LI`/`LBody`/`Span`/`Div`/`P` tags, URI and destination links, `show_text_glyphs`, PDF metadata/custom-metadata overwrite/removal, page labels, outline parent/child mixtures, PDF/PS page-size changes, PS DSC markers, SVG document units, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend lifecycle matrix | Done | Scene 40 adds a five-page lifecycle fixture with consecutive `Surface::copy_page` retention before a `Surface::show_page` clear, later fresh/final pages, PDF/PS page-size changes, PDF metadata/custom-metadata overwrite/removal, page labels, root/child/grandchild outline mixtures, URI and named destinations, deeper `Document`/`Part`/`Sect`/`H1`/`Figure`/`Art`/`P`/`Span`/`L`/`LI`/`Lbl`/`LBody`/`Table`/`TR`/`TD` tags, `show_text_glyphs`, PS Level 2 DSC markers, SVG millimeter document units, direct C output equivalence, file-vs-stream normalized equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend page-transition matrix | Done | Scene 41 covers a three-page cross-backend output with `Surface::show_page`, `Surface::copy_page`, and final `Surface::show_page` transitions; PDF metadata/custom-metadata removal, page labels, parent/child outlines, URI-link and named-destination tags, `Document`/`Sect`/`P`/`Figure`/`Span` structure tags, PDF/PS page-size changes, PS Level 3 DSC page-region markers, SVG point document units, direct C output equivalence, file-vs-stream normalized equality, stream-vs-direct-C oracle equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend state-stack matrix | Done | Scene 42 covers a three-page cross-backend output with clip/source-pattern state, group compositing, mask pattern painting, `Context::show_page`, and `Context::copy_page`; PDF metadata/custom-metadata removal, page labels, outlines, URI-link and named-destination tags, `Document`/`Sect`/`Figure`/`P`/`Span` structure tags, PDF/PS page-size changes, PS Level 3 DSC markers, SVG centimeter document units, direct C output equivalence, file-vs-stream normalized equality, stream-vs-direct-C oracle equality, stable marker assertions, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend text-state sequence | Done | Scene 44 covers a three-page cross-backend output with changing font matrices, `show_text`, `text_path`, `Surface::copy_page`, and `Surface::show_page`; PDF metadata/custom-metadata removal, page labels, root/child/sibling outlines, URI-link and named-destination tags, `Document`/`Sect`/`H1`/`P` structure tags, PDF/PS page-size changes, PS Level 3 DSC markers, SVG pixel document units, direct C output equivalence, file-vs-stream normalized equality, stream-vs-direct-C oracle equality, stable marker assertions, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend deep tag tree | Done | Scene 45 adds a focused two-page deep tag tree fixture with PDF metadata/custom-metadata overwrite/removal, page labels, outline parent/child links, URI and named-destination tags, `Document`/`Part`/`Sect`/`Div`/`P`/`Span`/`Figure`/`Table`/`TR`/`TH`/`TD` structure tags, direct C file-output equivalence, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, PDF stream marker coverage, PS/SVG stream-vs-file normalized equality, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend metadata outline matrix | Done | Scene 46 adds a focused three-page backend metadata/outline fixture with PDF metadata/custom-metadata overwrite/removal, page labels, root/child/sibling/grandchild outline flags, URI and named-destination links, `Document`/`Sect`/`H1` structure tags, retained `Surface::copy_page` output, PDF/PS page-size changes, PS Level 3 DSC setup/page markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend page-ops matrix | Done | Scene 47 adds a focused four-page backend page-operation fixture with mixed `Surface::show_page`, `Surface::copy_page`, `Context::copy_page`, and `Context::show_page` output, PDF metadata/custom-metadata overwrite/removal, page labels, outline flags, URI links, named destinations, `Document`/`Sect`/`P`/`Span` structure tags, PDF/PS page-size changes, PS DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend tag-metadata matrix | Done | Scene 48 adds a focused three-page tag/metadata/page-transition fixture with PDF metadata/custom-metadata overwrite/removal, page labels, outline parent/child links, URI links, named destinations, `Document`/`Sect`/`P`/`Figure`/`Span` structure tags, retained `Surface::copy_page` output, `Surface::show_page`, `Context::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend structure-sequence matrix | Done | Scene 49 adds a focused four-page structure/tag/page-operation fixture with retained `Surface::copy_page`, retained `Context::copy_page`, cleared `Surface::show_page`, and final `Context::show_page` output; PDF metadata/custom-metadata overwrite/removal, page labels, nested outline links, URI and named-destination tags, `Document`/`Part`/`Sect`/`L`/`LI`/`Lbl`/`LBody`/`Table`/`TR`/`TH`/`TD`/`Figure`/`P`/`Span` structure tags, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend outline-sequence matrix | Done | Scene 50 adds a focused three-page metadata/outline/page-operation fixture with `Context::copy_page`, `Surface::copy_page`, and `Surface::show_page` output; PDF metadata/custom-metadata overwrite/removal, page labels, root/child/sibling/grandchild outline flags, URI and named-destination tags, `Document`/`Sect`/`Figure` structure tags, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend pattern-tag matrix | Done | Scene 51 adds a focused three-page pattern/tag/font-matrix fixture with a transformed `Pattern::for_surface` source, radial mask, PDF metadata/custom-metadata removal, page labels, outline flags, URI and named-destination links, `Document`/`Sect`/`P`/`Figure`/`Div`/`Span` structure tags, retained `Surface::copy_page`, `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, `Context::set_font_matrix`, `ScaledFont::text_to_glyphs`, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend annotation sequence | Done | Scene 52 adds a focused three-page annotation/list/table fixture with PDF metadata/custom-metadata overwrite/removal, page labels, nested outlines, URI and destination links, `Document`/`Part`/`Sect`/`H1`/`P`/`L`/`LI`/`Lbl`/`LBody`/`Table`/`TR`/`TH`/`TD`/`Span` structure tags, retained `Surface::copy_page` output, PDF/PS page-size changes, PS Level 3 DSC markers, SVG pixel document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend semantic-index sequence | Done | Scene 53 adds a focused three-page semantic-index fixture with PDF metadata/custom-metadata overwrite/removal, page labels, nested outlines, destination and URI links, `Document`/`Part`/`Sect`/`H1`/`TOC`/`TOCI`/`Reference`/`Art`/`BlockQuote`/`P`/`Span`/`NonStruct`/`Note`/`Figure`/`Caption` structure tags, retained `Surface::copy_page` output, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend bookmark-lattice sequence | Done | Scene 54 adds a focused three-page bookmark-lattice fixture with PDF metadata/custom-metadata overwrite/removal, page labels, parent-child and root-sibling outlines, URI and named-destination links, `Document`/`Sect`/`H1`/`L`/`LI`/`Lbl`/`LBody`/`P`/`Span`/`Figure` structure tags, retained `Surface::copy_page` output, `Surface::show_page`, `Context::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend revision-ledger sequence | Done | Scene 55 adds a focused three-page revision-ledger fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, URI and named-destination links, `Document`/`Part`/`Sect`/`H1`/`P`/`Span`/`Table`/`TR`/`TD` structure tags, retained `Surface::copy_page` output, `Context::show_page`, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend article-thread sequence | Done | Scene 56 adds a focused three-page article-thread fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, URI and named-destination links, `Document`/`Art`/`Sect`/`P`/`Span`/`BlockQuote`/`Note`/`Figure`/`Caption`/`Reference` structure tags, retained `Surface::copy_page` output, `Context::show_page`, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend review-dossier sequence | Done | Scene 57 adds a focused three-page review-dossier fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, URI and named-destination links, `Document`/`Part`/`Sect`/`H1`/`TOC`/`TOCI`/`Reference`/`L`/`LI`/`Lbl`/`LBody`/`Table`/`TR`/`TH`/`TD`/`NonStruct`/`Note` structure tags, retained `Surface::copy_page` output, `Context::copy_page`, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend appendix-rubric sequence | Done | Scene 58 adds a focused three-page appendix-rubric fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, URI and named-destination links, `Document`/`Part`/`Sect`/`Index`/`Reference`/`Code`/`Formula`/`Figure`/`Caption`/`BlockQuote`/`BibEntry` structure tags, retained `Context::copy_page` output, retained `Surface::copy_page` output, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend research-note sequence | Done | Scene 59 adds a focused three-page research-note fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, URI and named-destination links, `Document`/`Sect`/`H1`/`P`/`Span`/`Table`/`TR`/`TH`/`TD`/`L`/`LI`/`Lbl`/`LBody`/`Note`/`Reference`/`Figure`/`Caption` structure tags, retained `Context::copy_page` output, retained `Surface::copy_page` output, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend cross-reference sequence | Done | Scene 60 adds a focused three-page cross-reference fixture with PDF metadata overwrite, custom-metadata overwrite/removal, page labels, nested outlines, named-destination and URI links, `Document`/`Sect`/`Index`/`Reference`/`Figure`/`Caption`/`P`/`Span` structure tags, first-page `Surface::show_page`, retained `Surface::copy_page`, final `Surface::show_page`, PDF/PS page-size changes, PS Level 3 DSC markers, SVG point document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG backend link-audit sequence | Done | Scene 61 adds a focused two-page link-audit fixture with PDF title/author/keywords/creator metadata, custom `AuditID` metadata, page labels, nested outlines, URI-link and named-destination tag output, `Document`/`Sect`/`P`/`L`/`LI`/`LBody` structure tags, PDF/PS page-size changes, PS Level 3 DSC markers, SVG pixel document units, direct C file-output equivalence, file-vs-stream normalized equality, PDF/PS/SVG stream-to-direct-C normalized oracle comparison, stable PDF/PS/SVG markers, and PS/SVG file/stream negative PDF-metadata checks |
| PDF/PS/SVG primitive stream-to-oracle output | Done | `surface_stream_oracle_test.mbt` compares stream callback bytes for scenes 0 through 14 directly with normalized direct C Cairo oracle files across PDF/PS/SVG. This covers paint, stroke, rectangle fill/stroke, Bezier path, transforms, linear and radial gradients, toy-font `text_path` and `show_text`, two-page output, clip, dash, surface pattern, mask surface, and mesh pattern vector output through the stream writer path. |
| PDF/PS/SVG single-backend stream-to-oracle output | Done | `surface_stream_oracle_test.mbt` compares stream callback bytes for scenes 18 through 21 and 34 directly with normalized direct C Cairo oracle files. This covers PDF metadata/custom metadata/page labels/outlines/URI/named-destination/document-structure feature output, PDF text-aware document feature output, PDF page-operation document feature output, PS DSC/page setup output, and SVG version/document-unit output through the stream writer path. |
| PDF MIME/thumbnail stream-to-oracle output | Done | `surface_stream_oracle_test.mbt` compares PDF stream callback bytes for JPEG MIME passthrough and page thumbnail output directly with normalized direct C Cairo oracle files. This covers `Surface::set_mime_data` JPEG embedding and `Surface::pdf_set_thumbnail_size` through the PDF stream writer path. |
| PDF/PS/SVG tag-heavy stream-to-oracle output | Done | `surface_stream_oracle_test.mbt` compares stream callback bytes for scenes 15 through 17 and 22 through 30 directly with normalized direct C Cairo oracle files across PDF/PS/SVG. This covers URI-link, destination-link, document-structure, text-tag, tagged multi-page, mixed-tag/text, layered, wide, `show_text_glyphs`, and grouped glyph/tag vector output through the stream writer path. |
| PDF/PS/SVG page/glyph stream-to-oracle output | Done | `surface_stream_oracle_test.mbt` compares stream callback bytes for scenes 31 through 33 directly with normalized direct C Cairo oracle files across PDF/PS/SVG. This covers the retained `Context::copy_page` vector scene plus `glyph_path` and `show_glyphs` vector scenes through the stream writer path. |
| PDF/PS/SVG tagged font-matrix text output | Done | Scene 43 covers a single-page tagged text-state fixture with `Context::set_font_matrix`, transformed `show_text`, transformed `text_path`, URI and destination tags, Document/Sect structure tags, direct C output equivalence, file-vs-stream normalized equality, stream-vs-direct-C oracle equality, stable PDF markers, and PS/SVG negative PDF-metadata checks |
| PDF/PS/SVG backend stream-to-oracle matrix | Done | `surface_stream_oracle_test.mbt` compares stream callback bytes for scenes 35 through 42 and 44 through 61 directly with normalized direct C Cairo oracle files across PDF/PS/SVG. This covers the backend stream-combo, surface-page combo, nested-tag, resized page-sequence, tag-matrix, lifecycle-matrix, page-transition, state-stack, text-state, deep-tag-tree, metadata-outline, page-ops, tag-metadata, structure-sequence, outline-sequence, pattern-tag, annotation-sequence, semantic-index, bookmark-lattice, revision-ledger, article-thread, review-dossier, appendix-rubric, research-note, cross-reference, and link-audit fixtures through the stream writer path, and the file is in the targeted normal/ASan verify gate. |

## Context APIs

| pycairo area | cairoon status | Notes |
|---|---|---|
| Construction and target lifetime | Done | `Context::new(Surface)` retains target surface; `Context::new_for_mapped_image(MappedImageSurface)` retains mapped image wrappers; pointer equality/hash and MoonBit `Eq`/`Hash` traits follow Cairo context identity; `get_target` returns a referenced `Surface` wrapper retaining the original target wrapper, and `get_group_target` returns a referenced `Surface` wrapper retaining the `Context` wrapper. Tests cover returned target, group target, source surface, mapped-image target, and PDF/PS stream target lifetime paths; targeted ASan covers this file, while broader clean ASan/LSan remains tracked in the Tests row because the current blocker is the macOS font/text leak report. |
| Save/restore/status | Done | Save/restore state behavior, initial `Success` status, sticky `InvalidRestore` status, and error mapping are tested |
| Paths | Done | move/rel-move/line/rel-line/curve/rel-curve/rectangle/arc/arc-negative, close/new path, current-point queries, and path data copying |
| Painting | Done | `paint`, `paint_with_alpha`, `fill`, `fill_preserve`, `stroke`, `stroke_preserve`, `mask`, and `mask_surface`; direct C ARGB32 oracle coverage includes non-zero `mask_surface` offsets and mask pattern compositing |
| Sources | Done | solid colors, explicit `Pattern`, `set_source_surface`, and `get_source` borrowed-return wrapper; tests cover pycairo-style `set_source_rgb/rgba` to `get_source().get_rgba()` round-trips, source-surface pixel sampling, non-zero source-surface offsets against a direct C ARGB32 oracle, invalid context errors, source surface lifetime after the original wrapper scope exits, and `get_source` surface-pattern lifetime after both source and context scopes exit |
| Transformations | Done | scale, translate, rotate, transform, user/device coordinate conversions |
| Clip APIs | Done | `clip`, `clip_preserve`, `reset_clip`, `clip_extents`, `in_clip`, and `copy_clip_rectangle_list`; tests cover pycairo's empty-path clip `in_clip` behavior, and clipped paint/fill output has direct C ARGB32 oracle coverage |
| Stroke/fill state | Done | line width/cap/join, miter limit, fill rule, tolerance, typed operator, pycairo-compatible raw operator/antialias/fill-rule/line-cap/line-join, antialias, dash, hairline mode; typed setter/getter round-trip tests cover every Antialias, FillRule, LineCap, LineJoin, and Operator value, pycairo parity covers `set_hairline`/`get_hairline`, raw operator tests cover pycairo's C-int passthrough for `-1`, `0`, `INT_MAX`, and `INT_MIN`, and raw drawing-state enum tests cover pycairo's C-int passthrough for unknown raw antialias/fill-rule/line-cap/line-join values while typed getters reject unknown raw values with `CairoInvalidArgument(InvalidStatus, _)`; dash/round-cap state, hairline stroke state, even-odd fill-rule output, and `OperatorClear` output also have direct C ARGB32 oracle coverage |
| Matrix APIs | Done | get/set/identity/translate/rotate/transform and user/device coordinate conversions |
| Groups | Done | `push_group`, `push_group_with_content`, `push_group_with_content_raw`, `pop_group`, `pop_group_to_source`, and `get_group_target` with pixel, raw C-int content, `InvalidContent`, group-target stack-restoration, unmatched-pop, and targeted ASan tests |
| Text and fonts | Partial | font options get/set including pycairo-compatible raw C-int antialias/subpixel/hint-style/hint-metrics/color-mode passthrough, pycairo-derived color mode, color palette, FontOptions variation set/clear, and custom palette invalid-index fixtures, font face get/set/select including raw C-int toy slant/weight parsing, toy font, font matrix/size/extents, toy text APIs, glyph arrays, `text_to_glyphs`, `text_to_glyphs_only`, and `show_text_glyphs` exist; context font-extents match the current scaled font; font family, FontOptions variations, and text APIs validate embedded NUL strings; context text/glyph wrappers are in the targeted ASan gate; ScaledFont font/text/glyph extents and text-to-glyph empty, single/multi/spaced ASCII, precomposed/decomposed Latin, CJK, Arabic RTL, and emoji UTF-8 coordinate cases have direct C primitive oracle coverage, image output has direct C ARGB32 oracle coverage for toy-font `text_path`, toy-font `show_text`, `glyph_path`, `show_glyphs`, and `show_text_glyphs`, PDF/PS/SVG toy-font text paths, toy-font `show_text`, `glyph_path`, `show_glyphs`, URI-link/named-destination/document-structure text tag scenes, tagged multi-page text, tagged `show_text_glyphs` scene, tagged font-matrix text scene, grouped multi-page glyph/tag scene, mixed vector/tag/text scenes, layered three-page clip/dash/surface-pattern/mask/tag/text scenes, wide three-page URI/destination/document-structure tag/vector scenes, backend-feature/tag stream-combo scenes, and the two-page deep tag tree text scene have direct C vector-output oracle coverage, deep tag tree text stream output now matches direct C after normalization across PDF/PS/SVG, standalone PDF/PS/SVG toy-font `text_path`/`show_text` scenes and tagged font-matrix text have stream-vs-file normalized equality coverage, and PDF tagged multi-page text plus tagged `show_text_glyphs`, tagged font-matrix text, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, backend-combo, and deep tag tree output have structure/tag marker coverage, while broader tag-aware vector-output coverage remains |
| Path copy/append | Done | `copy_path`, `copy_path_flat`, `append_path`, `Path::segments`, `Path::iter`, and `Path::to_string`; tests cover pycairo-style append string equivalence after the source context is cleared |
| Hit testing and extents | Done | `in_clip`, `in_fill`, `in_stroke`, `clip_extents`, `fill_extents`, `stroke_extents`, `path_extents` |
| Tags | Partial | `tag_begin`/`tag_end` exist with UTF-8/NUL validation, pycairo `test_tags` begin/end smoke coverage, tag constants, context-error propagation tests, unmatched/mismatched nesting `TagError` coverage, PDF 1.4 URI link-tag annotation marker coverage, PDF named-destination tag markers, PDF document-structure tag markers, single-page PDF URI/named-destination/document-structure rectangle and text tag scenes matched against direct C Cairo output, a combined two-page PDF URI/named-destination/document-structure tag scene matched against direct C Cairo output, PDF tagged multi-page text structure marker assertions, PDF rectangle tag marker assertions, PDF text tag marker assertions, PDF direct document-feature marker assertions, PDF tagged `show_text_glyphs` Link/structure marker assertions, PDF tagged font-matrix text marker assertions, PDF grouped glyph/tag marker assertions, PDF mixed vector/tag/text combined tag marker assertions, PDF layered three-page combined tag marker assertions, PDF wide three-page combined tag marker assertions, PDF backend-combo marker assertions, PDF deep tag tree marker assertions for `Document`/`Part`/`Sect`/`Div`/`P`/`Span`/`Figure`/`Table`/`TR`/`TH`/`TD`, PS/SVG Link inertness plus destination/document-structure rectangle and text tag scenes matched against direct C Cairo output, PS/SVG negative marker checks that URI-link text tags, destination/document-structure, tagged multi-page text, tagged `show_text_glyphs`, tagged font-matrix text, grouped glyph/tag multi-page, mixed vector/tag/text, layered three-page, wide three-page, backend-combo, and deep tag tree file/stream tag scenes do not emit PDF tag metadata, cross-backend tagged multi-page text, cross-backend tagged `show_text_glyphs`, cross-backend tagged font-matrix text, cross-backend grouped glyph/tag multi-page, cross-backend mixed vector/tag/text, cross-backend layered three-page tag/vector/text, cross-backend wide three-page tag/vector/text, cross-backend backend-feature/tag stream-combo scenes, and the cross-backend deep tag tree scene matched against direct C Cairo output, and PDF/PS/SVG stream-vs-file normalized equality for single-page URI-link, named-destination, document-structure rectangle/text tag scenes, tagged font-matrix text, plus backend-combo output, PDF/PS/SVG deep tag stream-to-direct-C normalized oracle comparison, PDF deep tag stream marker coverage, PS/SVG deep tag stream/file equality, and PS/SVG backend-combo/deep-tag stream negative tag-metadata checks; broader tag-aware normalized vector-output assertions remain |
| Page APIs | Done | `copy_page` and `show_page` on `Context` and `Surface`; `Context::copy_page` retained-page behavior has PDF/PS/SVG direct C vector-output oracle, PDF two-page marker, and stream-vs-file normalized equality coverage. `Surface::copy_page`/`Surface::show_page` retained-page behavior has a dedicated PDF/PS/SVG direct C oracle using `cairo_surface_copy_page`/`cairo_surface_show_page`, a PDF two-page marker check, and stream-vs-file normalized equality coverage, plus a backend feature/tag combo scene covering retained and cleared pages together with PDF metadata/page labels/outlines, PS DSC, SVG units, URI/destination tags, and document-structure tags. `Surface::show_page` cleared-page behavior has a dedicated PDF/PS/SVG direct C oracle using `cairo_surface_show_page`, a PDF two-page marker check, and stream-vs-file normalized equality coverage |

## Font And Text APIs

| pycairo API | cairoon status | Notes |
|---|---|---|
| `FontFace` | Done | External object with finalizer, status, pointer equality/hash, MoonBit `Eq`/`Hash` traits, and Context get/set |
| `ToyFontFace` | Done | Exposed as `FontFace::toy`/`toy_raw` plus family/slant/weight typed and raw accessors on `FontFace`; `Context::select_font_face_raw` mirrors pycairo's C-int slant/weight parsing and maps invalid raw values to `InvalidSlant`/`InvalidWeight` |
| `FontOptions` | Done | External object, copy/merge/equal/hash, MoonBit `Eq`/`Hash` traits, pycairo-derived variation set/clear fixture, variations with embedded-NUL rejection, color mode/palette/custom colors, antialias, subpixel, hint style, hint metrics, and pycairo-compatible raw C-int enum passthrough methods whose typed getters reject unknown raw values |
| `ScaledFont` | Done | Constructor, status, equality/hash, MoonBit `Eq`/`Hash` traits, extents, font face/options, font matrix, ctm, scale matrix, text extents, glyph extents, full text-to-glyph run, and glyph-only text conversion implemented; tests compare font/text/glyph extents plus empty, single/multi/spaced ASCII, precomposed/decomposed Latin, CJK, Arabic RTL, and emoji UTF-8 text-to-glyph coordinate cases against direct C Cairo oracles, and cover sheared font/CTM scale-matrix composition, embedded-NUL rejection for text extents, spaced ASCII cluster splitting, glyph-only parity through public APIs, executable Font reference docs, and Context get/set |
| `Glyph` arrays | Done | `ArrayView[Glyph]` marshaling uses `UInt64` index arrays for `Context::glyph_extents`, `Context::glyph_path`, `Context::show_glyphs`, `Context::show_text_glyphs`, `ScaledFont::glyph_extents`, `ScaledFont::text_to_glyphs` output, and `ScaledFont::text_to_glyphs_only`; C stubs reject indices that exceed `unsigned long` on narrower ABIs |
| `TextCluster` arrays | Done | `ArrayView[TextCluster]` marshaling exists for `Context::show_text_glyphs`; `ScaledFont::text_to_glyphs` returns copied `TextCluster` values and `ScaledFont::text_to_glyphs_only` covers pycairo's `with_clusters=False` semantics |
| UTF-8 text input | Done | Toy font family, context font-family inputs, FontOptions variations, `Context::show_text/text_extents/text_path/show_text_glyphs`, and `ScaledFont::text_extents/text_to_glyphs` encode to UTF-8 and reject embedded NUL |
| `FreeTypeFontFace` / `UserFontFace` | Decision | pycairo docs explicitly mark these Cairo font backends as not implemented and `cairo/__init__.pyi` exposes no public classes for them; native Cairo FreeType/user-font APIs are outside the pycairo-migration scope until cairoon deliberately grows a Cairo-extension layer |

## Region APIs

| pycairo API | cairoon status | Notes |
|---|---|---|
| `Region` | Done | External object, empty/single/multi-rectangle constructors, copy, extents, rectangle access with pycairo negative/positive invalid-index fixtures, predicates, empty-region overlap and translate behavior, equality plus MoonBit `Eq`, translate, copied-region lifetime after the source region exits, targeted normal/ASan gate coverage, and executable reference docs |
| Region boolean ops | Done | intersect/subtract/union/xor implemented for region and rectangle operands; pycairo-derived fixtures prove mutating boolean operations return the receiver; rectangle-op tests cover XOR split semantics and chainable return behavior |
| `RegionOverlap` | Done | Enum implemented |

## Product Decisions

These are resolved scope choices for the first full portable pycairo-compatible
MoonBit product.

Resolved by `AGENTS.md`: cairoon uses opaque MoonBit owner types with
constructor methods instead of CPython-style inheritance, and filename APIs
come before callback stream APIs.
Resolved by `AGENTS.md`: the first full-product scope targets portable Cairo
backends that can be built and verified in this workspace. Platform-native
Win32/XCB/Xlib surface APIs are out of that portable scope until cairoon grows a
separately designed conditional extension layer with matching platform headers,
link flags, and CI/test environments.
Resolved here: pycairo's `version`, `version_info`, and `get_include()`
packaging/C-extension helpers are out of cairoon's public runtime API.
Resolved here: pycairo's `CAPI` CPython capsule table is out of cairoon's
public API because cairoon exposes MoonBit-native wrappers rather than a Python
C-extension ABI.
Resolved here: pycairo's Python file-like object adapters are out of cairoon's
public API because MoonBit does not have Python file objects. Cairoon exposes
explicit stream callbacks, including PNG/vector/script stream APIs, and tests
those callbacks directly.
Resolved here: pycairo's legacy uppercase enum alias constants are not exposed
as MoonBit values. MoonBit uppercase names are primitive-only `const` values,
so enum-typed aliases cannot be represented without weakening the API to raw
integers; cairoon uses the typed enum constructors as the canonical API.
Resolved here: pycairo exposes `SurfaceObserverMode` but not Cairo's native
observer surface/device APIs, so those native observer APIs are out of the
pycairo-migration product scope until a separate Cairo-extension layer is
explicitly designed.
Resolved here: pycairo does not implement `FreeTypeFontFace` or `UserFontFace`
and does not expose public classes for them in `cairo/__init__.pyi`; cairoon
keeps those native Cairo font backends out of the pycairo-migration scope until
a separate extension layer is designed.

## Reliability Gates

Detailed execution rules live in `TESTING.md`.

- Every row above must move to Done or an explicit documented Decision before a
  full-product claim.
- `scripts/check-api-inventory.py` must pass; every public top-level entry in
  parent `cairo/__init__.pyi` must have an implementation or product-decision
  anchor in this file, and every portable pycairo class method must have a
  public MoonBit API anchor in `src/pkg.generated.mbti`.
- Each Done row must have black-box MoonBit tests and, where rendering is
  involved, deterministic pixel or vector-output assertions.
- Rendering APIs must gain differential tests against pycairo or Cairo C
  golden output before being called complete.
- Production `src/**/ffi*.mbt` declarations must pass
  `scripts/check-ffi-ownership.py`; every non-primitive C FFI parameter must be
  covered by `#borrow` or `#owned`.
- Every C-owned object must have an ASan/LSan run that exercises construction,
  normal use, error paths, and finalization.
- Public API changes must regenerate and review `src/pkg.generated.mbti`.
